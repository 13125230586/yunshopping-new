# 云商城系统完整技术方案文档

## 项目概述

### 1.1 项目信息

| 项目属性 | 信息 |
|---------|------|
| 项目名称 | 云商城系统 (YunShopping Mall System) |
| 后端项目名 | yunshopping-backend |
| 前端项目名 | yunshopping-frontend |
| 开发模式 | 前后端分离 |
| 参考项目 | yu-picture-backend / yu-picture-frontend |
| 技术类型 | 电商平台 - 多店铺B2C商城 |
| 版本号 | v1.0.0 |

### 1.2 项目背景

基于成熟的图片社区项目（yu-picture）的技术架构和最佳实践，设计开发一个功能完善的网上商城系统。项目将复用图片项目的：
- 技术栈和依赖配置
- 分层架构设计模式
- 权限控制机制
- 文件存储方案（腾讯云COS）
- 分页和审核流程设计

### 1.3 核心业务模块

```
云商城系统
├── 用户模块 (User Module)
│   ├── 买家用户管理
│   ├── 卖家用户管理
│   └── 管理员管理
├── 店铺模块 (Shop Module)
│   ├── 店铺入驻审核
│   ├── 店铺信息管理
│   └── 店铺等级管理
├── 商品模块 (Product Module)
│   ├── 商品发布与审核
│   ├── 商品分类与规格
│   ├── 库存管理
│   └── 商品搜索
├── 购物车模块 (Cart Module)
│   └── 购物车增删改查
├── 订单模块 (Order Module)
│   ├── 订单创建与支付
│   ├── 订单状态管理
│   └── 订单物流跟踪
├── 支付模块 (Payment Module)
│   ├── 支付宝集成
│   └── 微信支付集成
├── 评价模块 (Review Module)
│   ├── 商品评价
│   └── 店铺评分
└── 营销模块 (Marketing Module)
    ├── 优惠券管理
    ├── 秒杀活动
    └── 拼团活动
```

---

## 第一部分：技术栈设计

### 2.1 后端技术栈

| 技术 | 版本 | 用途 | 参考项目使用 |
|------|------|------|-------------|
| **核心框架** |
| Spring Boot | 2.7.6 | 应用框架 | ✅ |
| Java | 1.8 | 编程语言 | ✅ |
| Maven | 3.8+ | 项目管理 | ✅ |
| **数据持久化** |
| MyBatis Plus | 3.5.9 | ORM框架 | ✅ |
| MySQL | 8.0+ | 关系数据库 | ✅ |
| Redis | 7.0+ | 缓存/Session | ✅ |
| **Web框架** |
| Spring MVC | 2.7.6 | MVC框架 | ✅ |
| Spring Session | - | Session管理 | ✅ |
| Spring AOP | - | 切面编程 | ✅ |
| **文件存储** |
| 腾讯云COS | 5.6.227 | 对象存储（商品图片） | ✅ |
| **工具库** |
| Hutool | 5.8.26 | Java工具库 | ✅ |
| FastJSON | 1.2.76 | JSON序列化 | ✅ |
| Lombok | - | 简化代码 | ✅ |
| **接口文档** |
| Knife4j | 4.4.0 | API文档 | ✅ |
| **支付集成（新增）** |
| Alipay SDK | 4.38.0 | 支付宝支付 | ❌ 新增 |
| WxPay SDK | 3.0.10 | 微信支付 | ❌ 新增 |
| **消息队列（新增）** |
| RabbitMQ | 2.7.6 | 异步订单处理 | ❌ 新增 |

### 2.2 前端技术栈

| 技术 | 版本 | 用途 | 参考项目使用 |
|------|------|------|-------------|
| **核心框架** |
| Vue | 3.5.13 | 前端框架 | ✅ |
| TypeScript | 5.6.3 | 类型系统 | ✅ |
| **构建工具** |
| Vite | 6.0.1 | 构建工具 | ✅ |
| **路由和状态** |
| Vue Router | 4.4.5 | 路由管理 | ✅ |
| Pinia | 2.2.6 | 状态管理 | ✅ |
| **UI组件** |
| Ant Design Vue | 4.2.6 | UI组件库 | ✅ |
| **HTTP请求** |
| Axios | 1.12.2 | HTTP客户端 | ✅ |
| **其他工具** |
| vue-cropper | 1.1.4 | 图片裁剪 | ✅ |
| file-saver | 2.0.5 | 文件下载 | ✅ |
| **支付组件（新增）** |
| 支付宝前端SDK | - | 支付宝支付 | ❌ 新增 |
| 微信JSAPI | - | 微信支付 | ❌ 新增 |

---

## 第二部分：数据库设计

### 3.1 数据库设计原则

- 遵循第三范式设计
- 使用逻辑删除（isDelete字段）
- 统一时间字段（createTime、editTime、updateTime）
- 合理添加索引优化查询
- 使用BIGINT作为主键ID
- 采用UTF8MB4字符集

### 3.2 完整建表SQL

```sql
-- 创建数据库
CREATE DATABASE IF NOT EXISTS yunshopping DEFAULT CHARACTER SET utf8mb4 COLLATE utf8mb4_unicode_ci;

USE yunshopping;

-- ============================================
-- 1. 用户表 (user)
-- ============================================
CREATE TABLE IF NOT EXISTS `user` (
    `id` BIGINT AUTO_INCREMENT COMMENT '用户ID' PRIMARY KEY,
    `userAccount` VARCHAR(256) NOT NULL COMMENT '账号',
    `userPassword` VARCHAR(512) NOT NULL COMMENT '密码（加密）',
    `userName` VARCHAR(256) NULL COMMENT '用户昵称',
    `userAvatar` VARCHAR(1024) NULL COMMENT '用户头像URL',
    `userProfile` VARCHAR(512) NULL COMMENT '用户简介',
    `userRole` VARCHAR(256) DEFAULT 'buyer' NOT NULL COMMENT '用户角色：buyer-买家/seller-卖家/admin-管理员',
    `phone` VARCHAR(20) NULL COMMENT '手机号',
    `email` VARCHAR(256) NULL COMMENT '邮箱',
    `gender` TINYINT DEFAULT 0 NULL COMMENT '性别：0-未知 1-男 2-女',
    `birthday` DATE NULL COMMENT '生日',
    `defaultAddressId` BIGINT NULL COMMENT '默认收货地址ID',
    `editTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '编辑时间',
    `createTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '创建时间',
    `updateTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `isDelete` TINYINT DEFAULT 0 NOT NULL COMMENT '是否删除',
    UNIQUE KEY `uk_userAccount` (`userAccount`),
    INDEX `idx_userName` (`userName`),
    INDEX `idx_phone` (`phone`),
    INDEX `idx_userRole` (`userRole`)
) COMMENT '用户表' COLLATE = utf8mb4_unicode_ci;

-- ============================================
-- 2. 收货地址表 (address)
-- ============================================
CREATE TABLE IF NOT EXISTS `address` (
    `id` BIGINT AUTO_INCREMENT COMMENT '地址ID' PRIMARY KEY,
    `userId` BIGINT NOT NULL COMMENT '用户ID',
    `receiverName` VARCHAR(64) NOT NULL COMMENT '收货人姓名',
    `receiverPhone` VARCHAR(20) NOT NULL COMMENT '收货人手机号',
    `province` VARCHAR(64) NOT NULL COMMENT '省份',
    `city` VARCHAR(64) NOT NULL COMMENT '城市',
    `district` VARCHAR(64) NOT NULL COMMENT '区/县',
    `detailAddress` VARCHAR(512) NOT NULL COMMENT '详细地址',
    `postalCode` VARCHAR(20) NULL COMMENT '邮政编码',
    `isDefault` TINYINT DEFAULT 0 NOT NULL COMMENT '是否默认地址：0-否 1-是',
    `createTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '创建时间',
    `updateTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `isDelete` TINYINT DEFAULT 0 NOT NULL COMMENT '是否删除',
    INDEX `idx_userId` (`userId`)
) COMMENT '收货地址表' COLLATE = utf8mb4_unicode_ci;

-- ============================================
-- 3. 店铺表 (shop)
-- ============================================
CREATE TABLE IF NOT EXISTS `shop` (
    `id` BIGINT AUTO_INCREMENT COMMENT '店铺ID' PRIMARY KEY,
    `shopName` VARCHAR(128) NOT NULL COMMENT '店铺名称',
    `shopLogo` VARCHAR(1024) NULL COMMENT '店铺Logo URL',
    `shopBanner` VARCHAR(1024) NULL COMMENT '店铺横幅URL',
    `shopDescription` VARCHAR(1024) NULL COMMENT '店铺描述',
    `shopLevel` INT DEFAULT 0 NOT NULL COMMENT '店铺等级：0-普通店 1-品牌店 2-旗舰店',
    `shopStatus` INT DEFAULT 0 NOT NULL COMMENT '店铺状态：0-待审核 1-营业中 2-已关闭 3-审核拒绝',
    `userId` BIGINT NOT NULL COMMENT '店主用户ID',
    `totalSales` BIGINT DEFAULT 0 NULL COMMENT '总销量',
    `totalRevenue` DECIMAL(10,2) DEFAULT 0.00 NULL COMMENT '总营收',
    `rating` DECIMAL(3,2) DEFAULT 5.00 NULL COMMENT '店铺评分（1-5分）',
    `reviewCount` INT DEFAULT 0 NULL COMMENT '评价数量',
    `province` VARCHAR(64) NULL COMMENT '店铺所在省份',
    `city` VARCHAR(64) NULL COMMENT '店铺所在城市',
    `reviewStatus` INT DEFAULT 0 NOT NULL COMMENT '审核状态：0-待审核 1-通过 2-拒绝',
    `reviewMessage` VARCHAR(512) NULL COMMENT '审核信息',
    `reviewerId` BIGINT NULL COMMENT '审核人ID',
    `reviewTime` DATETIME NULL COMMENT '审核时间',
    `createTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '创建时间',
    `editTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '编辑时间',
    `updateTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `isDelete` TINYINT DEFAULT 0 NOT NULL COMMENT '是否删除',
    INDEX `idx_userId` (`userId`),
    INDEX `idx_shopName` (`shopName`),
    INDEX `idx_shopLevel` (`shopLevel`),
    INDEX `idx_shopStatus` (`shopStatus`),
    INDEX `idx_reviewStatus` (`reviewStatus`)
) COMMENT '店铺表' COLLATE = utf8mb4_unicode_ci;

-- ============================================
-- 4. 商品分类表 (category)
-- ============================================
CREATE TABLE IF NOT EXISTS `category` (
    `id` BIGINT AUTO_INCREMENT COMMENT '分类ID' PRIMARY KEY,
    `categoryName` VARCHAR(128) NOT NULL COMMENT '分类名称',
    `parentId` BIGINT DEFAULT 0 NOT NULL COMMENT '父分类ID（0表示一级分类）',
    `level` TINYINT DEFAULT 1 NOT NULL COMMENT '分类层级：1-一级 2-二级 3-三级',
    `sortOrder` INT DEFAULT 0 NULL COMMENT '排序序号',
    `icon` VARCHAR(512) NULL COMMENT '分类图标URL',
    `createTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '创建时间',
    `updateTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `isDelete` TINYINT DEFAULT 0 NOT NULL COMMENT '是否删除',
    INDEX `idx_parentId` (`parentId`),
    INDEX `idx_level` (`level`)
) COMMENT '商品分类表' COLLATE = utf8mb4_unicode_ci;

-- ============================================
-- 5. 商品表 (product)
-- ============================================
CREATE TABLE IF NOT EXISTS `product` (
    `id` BIGINT AUTO_INCREMENT COMMENT '商品ID' PRIMARY KEY,
    `productName` VARCHAR(256) NOT NULL COMMENT '商品名称',
    `productTitle` VARCHAR(512) NULL COMMENT '商品标题',
    `productDescription` TEXT NULL COMMENT '商品描述',
    `categoryId` BIGINT NOT NULL COMMENT '分类ID',
    `shopId` BIGINT NOT NULL COMMENT '店铺ID',
    `brandName` VARCHAR(128) NULL COMMENT '品牌名称',
    `mainImageUrl` VARCHAR(1024) NOT NULL COMMENT '商品主图URL',
    `imageUrls` TEXT NULL COMMENT '商品图片URL列表（JSON数组）',
    `price` DECIMAL(10,2) NOT NULL COMMENT '商品价格',
    `originalPrice` DECIMAL(10,2) NULL COMMENT '原价',
    `stock` INT DEFAULT 0 NOT NULL COMMENT '库存数量',
    `sales` INT DEFAULT 0 NULL COMMENT '销量',
    `unit` VARCHAR(32) DEFAULT '件' NULL COMMENT '单位',
    `weight` DECIMAL(10,2) NULL COMMENT '重量（kg）',
    `tags` VARCHAR(512) NULL COMMENT '标签（JSON数组）',
    `specifications` TEXT NULL COMMENT '商品规格（JSON）',
    `status` INT DEFAULT 0 NOT NULL COMMENT '商品状态：0-待上架 1-已上架 2-已下架',
    `reviewStatus` INT DEFAULT 0 NOT NULL COMMENT '审核状态：0-待审核 1-通过 2-拒绝',
    `reviewMessage` VARCHAR(512) NULL COMMENT '审核信息',
    `reviewerId` BIGINT NULL COMMENT '审核人ID',
    `reviewTime` DATETIME NULL COMMENT '审核时间',
    `userId` BIGINT NOT NULL COMMENT '发布者用户ID',
    `viewCount` INT DEFAULT 0 NULL COMMENT '浏览次数',
    `favoriteCount` INT DEFAULT 0 NULL COMMENT '收藏次数',
    `createTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '创建时间',
    `editTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '编辑时间',
    `updateTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `isDelete` TINYINT DEFAULT 0 NOT NULL COMMENT '是否删除',
    INDEX `idx_productName` (`productName`),
    INDEX `idx_categoryId` (`categoryId`),
    INDEX `idx_shopId` (`shopId`),
    INDEX `idx_userId` (`userId`),
    INDEX `idx_status` (`status`),
    INDEX `idx_reviewStatus` (`reviewStatus`),
    INDEX `idx_price` (`price`)
) COMMENT '商品表' COLLATE = utf8mb4_unicode_ci;

-- ============================================
-- 6. 购物车表 (cart)
-- ============================================
CREATE TABLE IF NOT EXISTS `cart` (
    `id` BIGINT AUTO_INCREMENT COMMENT '购物车ID' PRIMARY KEY,
    `userId` BIGINT NOT NULL COMMENT '用户ID',
    `productId` BIGINT NOT NULL COMMENT '商品ID',
    `quantity` INT DEFAULT 1 NOT NULL COMMENT '商品数量',
    `specification` VARCHAR(512) NULL COMMENT '选中的规格（JSON）',
    `isChecked` TINYINT DEFAULT 1 NOT NULL COMMENT '是否选中：0-否 1-是',
    `createTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '创建时间',
    `updateTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX `idx_userId` (`userId`),
    INDEX `idx_productId` (`productId`),
    UNIQUE KEY `uk_user_product` (`userId`, `productId`, `specification`(255))
) COMMENT '购物车表' COLLATE = utf8mb4_unicode_ci;

-- ============================================
-- 7. 订单表 (orders)
-- ============================================
CREATE TABLE IF NOT EXISTS `orders` (
    `id` BIGINT AUTO_INCREMENT COMMENT '订单ID' PRIMARY KEY,
    `orderNo` VARCHAR(64) NOT NULL COMMENT '订单号（唯一）',
    `userId` BIGINT NOT NULL COMMENT '用户ID',
    `shopId` BIGINT NOT NULL COMMENT '店铺ID',
    `totalAmount` DECIMAL(10,2) NOT NULL COMMENT '订单总金额',
    `payAmount` DECIMAL(10,2) NOT NULL COMMENT '实付金额',
    `discountAmount` DECIMAL(10,2) DEFAULT 0.00 NULL COMMENT '优惠金额',
    `shippingFee` DECIMAL(10,2) DEFAULT 0.00 NULL COMMENT '运费',
    `paymentMethod` VARCHAR(32) NULL COMMENT '支付方式：alipay-支付宝 wxpay-微信',
    `orderStatus` INT DEFAULT 0 NOT NULL COMMENT '订单状态：0-待支付 1-已支付 2-待发货 3-已发货 4-已完成 5-已取消 6-退款中 7-已退款',
    `paymentStatus` INT DEFAULT 0 NOT NULL COMMENT '支付状态：0-未支付 1-已支付 2-已退款',
    `shippingStatus` INT DEFAULT 0 NOT NULL COMMENT '物流状态：0-未发货 1-已发货 2-已签收',
    `receiverName` VARCHAR(64) NOT NULL COMMENT '收货人姓名',
    `receiverPhone` VARCHAR(20) NOT NULL COMMENT '收货人手机号',
    `receiverAddress` VARCHAR(512) NOT NULL COMMENT '收货地址',
    `buyerMessage` VARCHAR(512) NULL COMMENT '买家留言',
    `payTime` DATETIME NULL COMMENT '支付时间',
    `shipTime` DATETIME NULL COMMENT '发货时间',
    `completeTime` DATETIME NULL COMMENT '完成时间',
    `cancelTime` DATETIME NULL COMMENT '取消时间',
    `createTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '创建时间',
    `updateTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `isDelete` TINYINT DEFAULT 0 NOT NULL COMMENT '是否删除',
    UNIQUE KEY `uk_orderNo` (`orderNo`),
    INDEX `idx_userId` (`userId`),
    INDEX `idx_shopId` (`shopId`),
    INDEX `idx_orderStatus` (`orderStatus`),
    INDEX `idx_createTime` (`createTime`)
) COMMENT '订单表' COLLATE = utf8mb4_unicode_ci;

-- ============================================
-- 8. 订单详情表 (order_item)
-- ============================================
CREATE TABLE IF NOT EXISTS `order_item` (
    `id` BIGINT AUTO_INCREMENT COMMENT '订单详情ID' PRIMARY KEY,
    `orderId` BIGINT NOT NULL COMMENT '订单ID',
    `productId` BIGINT NOT NULL COMMENT '商品ID',
    `productName` VARCHAR(256) NOT NULL COMMENT '商品名称',
    `productImage` VARCHAR(1024) NULL COMMENT '商品图片',
    `price` DECIMAL(10,2) NOT NULL COMMENT '商品单价',
    `quantity` INT NOT NULL COMMENT '购买数量',
    `totalAmount` DECIMAL(10,2) NOT NULL COMMENT '小计金额',
    `specification` VARCHAR(512) NULL COMMENT '商品规格（JSON）',
    `createTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '创建时间',
    `updateTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX `idx_orderId` (`orderId`),
    INDEX `idx_productId` (`productId`)
) COMMENT '订单详情表' COLLATE = utf8mb4_unicode_ci;

-- ============================================
-- 9. 支付记录表 (payment)
-- ============================================
CREATE TABLE IF NOT EXISTS `payment` (
    `id` BIGINT AUTO_INCREMENT COMMENT '支付ID' PRIMARY KEY,
    `paymentNo` VARCHAR(64) NOT NULL COMMENT '支付流水号',
    `orderId` BIGINT NOT NULL COMMENT '订单ID',
    `userId` BIGINT NOT NULL COMMENT '用户ID',
    `paymentMethod` VARCHAR(32) NOT NULL COMMENT '支付方式',
    `payAmount` DECIMAL(10,2) NOT NULL COMMENT '支付金额',
    `payStatus` INT DEFAULT 0 NOT NULL COMMENT '支付状态：0-待支付 1-已支付 2-已退款',
    `transactionId` VARCHAR(128) NULL COMMENT '第三方交易流水号',
    `payTime` DATETIME NULL COMMENT '支付时间',
    `refundTime` DATETIME NULL COMMENT '退款时间',
    `createTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '创建时间',
    `updateTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    UNIQUE KEY `uk_paymentNo` (`paymentNo`),
    INDEX `idx_orderId` (`orderId`),
    INDEX `idx_userId` (`userId`),
    INDEX `idx_transactionId` (`transactionId`)
) COMMENT '支付记录表' COLLATE = utf8mb4_unicode_ci;

-- ============================================
-- 10. 物流表 (logistics)
-- ============================================
CREATE TABLE IF NOT EXISTS `logistics` (
    `id` BIGINT AUTO_INCREMENT COMMENT '物流ID' PRIMARY KEY,
    `orderId` BIGINT NOT NULL COMMENT '订单ID',
    `logisticsCompany` VARCHAR(128) NULL COMMENT '物流公司',
    `trackingNumber` VARCHAR(128) NULL COMMENT '物流单号',
    `logisticsStatus` INT DEFAULT 0 NOT NULL COMMENT '物流状态：0-未发货 1-运输中 2-派送中 3-已签收',
    `currentLocation` VARCHAR(256) NULL COMMENT '当前位置',
    `logisticsInfo` TEXT NULL COMMENT '物流信息（JSON数组）',
    `shipTime` DATETIME NULL COMMENT '发货时间',
    `receiveTime` DATETIME NULL COMMENT '签收时间',
    `createTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '创建时间',
    `updateTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX `idx_orderId` (`orderId`),
    INDEX `idx_trackingNumber` (`trackingNumber`)
) COMMENT '物流表' COLLATE = utf8mb4_unicode_ci;

-- ============================================
-- 11. 商品评价表 (review)
-- ============================================
CREATE TABLE IF NOT EXISTS `review` (
    `id` BIGINT AUTO_INCREMENT COMMENT '评价ID' PRIMARY KEY,
    `userId` BIGINT NOT NULL COMMENT '用户ID',
    `productId` BIGINT NOT NULL COMMENT '商品ID',
    `orderId` BIGINT NOT NULL COMMENT '订单ID',
    `shopId` BIGINT NOT NULL COMMENT '店铺ID',
    `rating` TINYINT NOT NULL COMMENT '评分：1-5星',
    `content` TEXT NULL COMMENT '评价内容',
    `images` TEXT NULL COMMENT '评价图片（JSON数组）',
    `isAnonymous` TINYINT DEFAULT 0 NOT NULL COMMENT '是否匿名：0-否 1-是',
    `likeCount` INT DEFAULT 0 NULL COMMENT '点赞数',
    `replyContent` TEXT NULL COMMENT '商家回复',
    `replyTime` DATETIME NULL COMMENT '回复时间',
    `createTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '创建时间',
    `updateTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `isDelete` TINYINT DEFAULT 0 NOT NULL COMMENT '是否删除',
    INDEX `idx_userId` (`userId`),
    INDEX `idx_productId` (`productId`),
    INDEX `idx_orderId` (`orderId`),
    INDEX `idx_shopId` (`shopId`)
) COMMENT '商品评价表' COLLATE = utf8mb4_unicode_ci;

-- ============================================
-- 12. 优惠券表 (coupon)
-- ============================================
CREATE TABLE IF NOT EXISTS `coupon` (
    `id` BIGINT AUTO_INCREMENT COMMENT '优惠券ID' PRIMARY KEY,
    `couponName` VARCHAR(128) NOT NULL COMMENT '优惠券名称',
    `couponType` INT NOT NULL COMMENT '优惠券类型：1-满减 2-折扣 3-无门槛',
    `discountAmount` DECIMAL(10,2) NULL COMMENT '优惠金额（满减和无门槛使用）',
    `discountRate` DECIMAL(3,2) NULL COMMENT '折扣率（折扣券使用，如0.8表示8折）',
    `minAmount` DECIMAL(10,2) DEFAULT 0.00 NULL COMMENT '使用门槛（满X元可用）',
    `totalCount` INT NOT NULL COMMENT '发行总数',
    `usedCount` INT DEFAULT 0 NULL COMMENT '已使用数量',
    `shopId` BIGINT NULL COMMENT '店铺ID（NULL表示平台券）',
    `startTime` DATETIME NOT NULL COMMENT '生效时间',
    `endTime` DATETIME NOT NULL COMMENT '失效时间',
    `status` INT DEFAULT 1 NOT NULL COMMENT '状态：0-未开始 1-进行中 2-已结束',
    `createTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '创建时间',
    `updateTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `isDelete` TINYINT DEFAULT 0 NOT NULL COMMENT '是否删除',
    INDEX `idx_shopId` (`shopId`),
    INDEX `idx_status` (`status`),
    INDEX `idx_time` (`startTime`, `endTime`)
) COMMENT '优惠券表' COLLATE = utf8mb4_unicode_ci;

-- ============================================
-- 13. 用户优惠券表 (user_coupon)
-- ============================================
CREATE TABLE IF NOT EXISTS `user_coupon` (
    `id` BIGINT AUTO_INCREMENT COMMENT '用户优惠券ID' PRIMARY KEY,
    `userId` BIGINT NOT NULL COMMENT '用户ID',
    `couponId` BIGINT NOT NULL COMMENT '优惠券ID',
    `orderId` BIGINT NULL COMMENT '使用的订单ID',
    `status` INT DEFAULT 0 NOT NULL COMMENT '状态：0-未使用 1-已使用 2-已过期',
    `receiveTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '领取时间',
    `useTime` DATETIME NULL COMMENT '使用时间',
    `createTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '创建时间',
    `updateTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    INDEX `idx_userId` (`userId`),
    INDEX `idx_couponId` (`couponId`),
    INDEX `idx_status` (`status`)
) COMMENT '用户优惠券表' COLLATE = utf8mb4_unicode_ci;

-- ============================================
-- 14. 秒杀活动表 (seckill)
-- ============================================
CREATE TABLE IF NOT EXISTS `seckill` (
    `id` BIGINT AUTO_INCREMENT COMMENT '秒杀活动ID' PRIMARY KEY,
    `activityName` VARCHAR(128) NOT NULL COMMENT '活动名称',
    `productId` BIGINT NOT NULL COMMENT '商品ID',
    `seckillPrice` DECIMAL(10,2) NOT NULL COMMENT '秒杀价格',
    `seckillStock` INT NOT NULL COMMENT '秒杀库存',
    `remainStock` INT NOT NULL COMMENT '剩余库存',
    `limitPerUser` INT DEFAULT 1 NOT NULL COMMENT '每人限购数量',
    `startTime` DATETIME NOT NULL COMMENT '开始时间',
    `endTime` DATETIME NOT NULL COMMENT '结束时间',
    `status` INT DEFAULT 0 NOT NULL COMMENT '状态：0-未开始 1-进行中 2-已结束',
    `createTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '创建时间',
    `updateTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL ON UPDATE CURRENT_TIMESTAMP COMMENT '更新时间',
    `isDelete` TINYINT DEFAULT 0 NOT NULL COMMENT '是否删除',
    INDEX `idx_productId` (`productId`),
    INDEX `idx_status` (`status`),
    INDEX `idx_time` (`startTime`, `endTime`)
) COMMENT '秒杀活动表' COLLATE = utf8mb4_unicode_ci;

-- ============================================
-- 15. 收藏表 (favorite)
-- ============================================
CREATE TABLE IF NOT EXISTS `favorite` (
    `id` BIGINT AUTO_INCREMENT COMMENT '收藏ID' PRIMARY KEY,
    `userId` BIGINT NOT NULL COMMENT '用户ID',
    `productId` BIGINT NOT NULL COMMENT '商品ID',
    `createTime` DATETIME DEFAULT CURRENT_TIMESTAMP NOT NULL COMMENT '创建时间',
    INDEX `idx_userId` (`userId`),
    INDEX `idx_productId` (`productId`),
    UNIQUE KEY `uk_user_product` (`userId`, `productId`)
) COMMENT '收藏表' COLLATE = utf8mb4_unicode_ci;
```

### 3.3 数据库ER图说明

**核心实体关系**：

```
User (用户)
  ├── 1:N → Address (收货地址)
  ├── 1:N → Shop (店铺)
  ├── 1:N → Cart (购物车)
  ├── 1:N → Orders (订单)
  ├── 1:N → Review (评价)
  ├── 1:N → UserCoupon (用户优惠券)
  └── 1:N → Favorite (收藏)

Shop (店铺)
  ├── 1:N → Product (商品)
  ├── 1:N → Orders (订单)
  ├── 1:N → Review (评价)
  └── 1:N → Coupon (优惠券)

Product (商品)
  ├── N:1 → Category (分类)
  ├── N:1 → Shop (店铺)
  ├── 1:N → Cart (购物车)
  ├── 1:N → OrderItem (订单详情)
  ├── 1:N → Review (评价)
  ├── 1:N → Seckill (秒杀活动)
  └── 1:N → Favorite (收藏)

Orders (订单)
  ├── 1:N → OrderItem (订单详情)
  ├── 1:1 → Payment (支付记录)
  ├── 1:1 → Logistics (物流)
  └── 1:N → Review (评价)
```

---

## 第三部分：后端架构设计

### 4.1 项目目录结构

```
yunshopping-backend/
├── src/main/java/com/yupi/yunshoppingbackend/
│   ├── YunShoppingBackendApplication.java          # 启动类
│   ├── annotation/                                  # 自定义注解
│   │   └── AuthCheck.java                          # 权限校验注解
│   ├── aop/                                        # AOP切面
│   │   └── AuthInterceptor.java                    # 权限拦截切面
│   ├── common/                                     # 公共模块
│   │   ├── BaseResponse.java                       # 统一响应封装
│   │   ├── ResultUtils.java                        # 响应工具类
│   │   ├── PageRequest.java                        # 分页请求
│   │   └── DeleteRequest.java                      # 删除请求
│   ├── config/                                     # 配置类
│   │   ├── CorsConfig.java                         # CORS跨域配置
│   │   ├── CosClientConfig.java                    # 腾讯云COS配置
│   │   ├── JsonConfig.java                         # JSON配置
│   │   ├── MyBatisPlusConfig.java                  # MyBatis Plus配置
│   │   ├── RedisConfig.java                        # Redis配置
│   │   ├── AlipayConfig.java                       # 支付宝配置（新增）
│   │   ├── WxPayConfig.java                        # 微信支付配置（新增）
│   │   └── RabbitMQConfig.java                     # RabbitMQ配置（新增）
│   ├── constant/                                   # 常量
│   │   ├── UserConstant.java                       # 用户常量
│   │   ├── OrderConstant.java                      # 订单常量（新增）
│   │   ├── PaymentConstant.java                    # 支付常量（新增）
│   │   └── ShopConstant.java                       # 店铺常量（新增）
│   ├── controller/                                 # 控制层
│   │   ├── UserController.java                     # 用户管理接口
│   │   ├── AddressController.java                  # 地址管理接口（新增）
│   │   ├── ShopController.java                     # 店铺管理接口（新增）
│   │   ├── CategoryController.java                 # 分类管理接口（新增）
│   │   ├── ProductController.java                  # 商品管理接口（新增）
│   │   ├── CartController.java                     # 购物车接口（新增）
│   │   ├── OrderController.java                    # 订单管理接口（新增）
│   │   ├── PaymentController.java                  # 支付接口（新增）
│   │   ├── ReviewController.java                   # 评价接口（新增）
│   │   ├── CouponController.java                   # 优惠券接口（新增）
│   │   ├── SeckillController.java                  # 秒杀接口（新增）
│   │   ├── FileController.java                     # 文件上传接口
│   │   └── MainController.java                     # 主页接口
│   ├── exception/                                  # 异常处理
│   │   ├── ErrorCode.java                          # 错误码枚举
│   │   ├── BusinessException.java                  # 业务异常
│   │   ├── GlobalExceptionHandler.java             # 全局异常处理
│   │   └── ThrowUtils.java                         # 异常抛出工具
│   ├── manager/                                    # 管理器层
│   │   ├── CosManager.java                         # 腾讯云COS管理
│   │   ├── FileManager.java                        # 文件管理
│   │   ├── OrderManager.java                       # 订单管理器（新增）
│   │   ├── PaymentManager.java                     # 支付管理器（新增）
│   │   └── upload/
│   │       ├── ImageUploadTemplate.java            # 图片上传模板
│   │       ├── FileImageUpload.java                # 本地文件上传
│   │       └── UrlImageUpload.java                 # URL图片上传
│   ├── mapper/                                     # 数据库映射层
│   │   ├── UserMapper.java
│   │   ├── AddressMapper.java                      # 新增
│   │   ├── ShopMapper.java                         # 新增
│   │   ├── CategoryMapper.java                     # 新增
│   │   ├── ProductMapper.java                      # 新增
│   │   ├── CartMapper.java                         # 新增
│   │   ├── OrderMapper.java                        # 新增
│   │   ├── OrderItemMapper.java                    # 新增
│   │   ├── PaymentMapper.java                      # 新增
│   │   ├── LogisticsMapper.java                    # 新增
│   │   ├── ReviewMapper.java                       # 新增
│   │   ├── CouponMapper.java                       # 新增
│   │   ├── UserCouponMapper.java                   # 新增
│   │   ├── SeckillMapper.java                      # 新增
│   │   └── FavoriteMapper.java                     # 新增
│   ├── model/                                      # 数据模型
│   │   ├── entity/                                 # 数据库实体（15个表对应15个实体）
│   │   │   ├── User.java
│   │   │   ├── Address.java
│   │   │   ├── Shop.java
│   │   │   ├── Category.java
│   │   │   ├── Product.java
│   │   │   ├── Cart.java
│   │   │   ├── Order.java
│   │   │   ├── OrderItem.java
│   │   │   ├── Payment.java
│   │   │   ├── Logistics.java
│   │   │   ├── Review.java
│   │   │   ├── Coupon.java
│   │   │   ├── UserCoupon.java
│   │   │   ├── Seckill.java
│   │   │   └── Favorite.java
│   │   ├── dto/                                    # 数据传输对象
│   │   │   ├── user/                               # 用户相关DTO
│   │   │   ├── shop/                               # 店铺相关DTO
│   │   │   ├── product/                            # 商品相关DTO
│   │   │   ├── cart/                               # 购物车相关DTO
│   │   │   ├── order/                              # 订单相关DTO
│   │   │   ├── payment/                            # 支付相关DTO
│   │   │   ├── review/                             # 评价相关DTO
│   │   │   ├── coupon/                             # 优惠券相关DTO
│   │   │   └── file/                               # 文件相关DTO
│   │   ├── vo/                                     # 视图对象（响应）
│   │   │   ├── UserVO.java
│   │   │   ├── ShopVO.java
│   │   │   ├── ProductVO.java
│   │   │   ├── CartVO.java
│   │   │   ├── OrderVO.java
│   │   │   ├── ReviewVO.java
│   │   │   └── ...
│   │   └── enums/                                  # 枚举类
│   │       ├── UserRoleEnum.java                   # 用户角色枚举
│   │       ├── ShopStatusEnum.java                 # 店铺状态枚举
│   │       ├── ProductStatusEnum.java              # 商品状态枚举
│   │       ├── OrderStatusEnum.java                # 订单状态枚举
│   │       ├── PaymentStatusEnum.java              # 支付状态枚举
│   │       └── ReviewStatusEnum.java               # 审核状态枚举
│   ├── service/                                    # 业务逻辑层
│   │   ├── UserService.java
│   │   ├── AddressService.java
│   │   ├── ShopService.java
│   │   ├── CategoryService.java
│   │   ├── ProductService.java
│   │   ├── CartService.java
│   │   ├── OrderService.java
│   │   ├── PaymentService.java
│   │   ├── LogisticsService.java
│   │   ├── ReviewService.java
│   │   ├── CouponService.java
│   │   ├── SeckillService.java
│   │   ├── FavoriteService.java
│   │   └── impl/                                   # 服务实现
│   │       ├── UserServiceImpl.java
│   │       ├── ...
│   │       └── SeckillServiceImpl.java
│   └── utils/                                      # 工具类
│       ├── OrderNumberUtil.java                    # 订单号生成工具
│       ├── EncryptUtil.java                        # 加密工具
│       └── PriceUtil.java                          # 价格计算工具
├── src/main/resources/
│   ├── application.yml                             # 主配置文件
│   ├── application-local.yml                       # 本地开发配置
│   ├── mapper/                                     # MyBatis XML映射文件
│   │   ├── UserMapper.xml
│   │   ├── ...
│   │   └── SeckillMapper.xml
│   └── static/                                     # 静态资源
├── sql/
│   └── create_table.sql                            # 数据库DDL脚本
└── pom.xml                                         # Maven配置文件
```

### 4.2 完整 pom.xml 配置

```xml
<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0
         https://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <groupId>com.yupi</groupId>
    <artifactId>yunshopping-backend</artifactId>
    <version>1.0.0</version>
    <name>yunshopping-backend</name>
    <description>云商城系统后端</description>

    <properties>
        <java.version>1.8</java.version>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
        <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>
        <spring-boot.version>2.7.6</spring-boot.version>
    </properties>

    <dependencies>
        <!-- Spring Boot Web -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>

        <!-- Redis -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-data-redis</artifactId>
        </dependency>

        <!-- Spring Session + Redis -->
        <dependency>
            <groupId>org.springframework.session</groupId>
            <artifactId>spring-session-data-redis</artifactId>
        </dependency>

        <!-- Spring AOP 切面编程 -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-aop</artifactId>
        </dependency>

        <!-- MyBatis Plus -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-boot-starter</artifactId>
            <version>3.5.9</version>
        </dependency>

        <!-- Hutool 工具库 -->
        <dependency>
            <groupId>cn.hutool</groupId>
            <artifactId>hutool-all</artifactId>
            <version>5.8.26</version>
        </dependency>

        <!-- 腾讯云 COS 对象存储 -->
        <dependency>
            <groupId>com.qcloud</groupId>
            <artifactId>cos_api</artifactId>
            <version>5.6.227</version>
        </dependency>

        <!-- Knife4j 接口文档 -->
        <dependency>
            <groupId>com.github.xiaoymin</groupId>
            <artifactId>knife4j-openapi2-spring-boot-starter</artifactId>
            <version>4.4.0</version>
        </dependency>

        <!-- MyBatis Plus 分页插件 -->
        <dependency>
            <groupId>com.baomidou</groupId>
            <artifactId>mybatis-plus-jsqlparser-4.9</artifactId>
        </dependency>

        <!-- FastJSON -->
        <dependency>
            <groupId>com.alibaba</groupId>
            <artifactId>fastjson</artifactId>
            <version>1.2.76</version>
        </dependency>

        <!-- JSoup HTML解析 -->
        <dependency>
            <groupId>org.jsoup</groupId>
            <artifactId>jsoup</artifactId>
            <version>1.15.3</version>
        </dependency>

        <!-- MySQL驱动 -->
        <dependency>
            <groupId>com.mysql</groupId>
            <artifactId>mysql-connector-j</artifactId>
            <scope>runtime</scope>
        </dependency>

        <!-- Lombok -->
        <dependency>
            <groupId>org.projectlombok</groupId>
            <artifactId>lombok</artifactId>
            <optional>true</optional>
        </dependency>

        <!-- ========== 新增依赖 ========== -->

        <!-- 支付宝SDK -->
        <dependency>
            <groupId>com.alipay.sdk</groupId>
            <artifactId>alipay-sdk-java</artifactId>
            <version>4.38.0.ALL</version>
        </dependency>

        <!-- 微信支付SDK -->
        <dependency>
            <groupId>com.github.wechatpay-apiv3</groupId>
            <artifactId>wechatpay-apache-httpclient</artifactId>
            <version>0.4.9</version>
        </dependency>

        <!-- RabbitMQ -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-amqp</artifactId>
        </dependency>

        <!-- Spring Boot Test -->
        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-test</artifactId>
            <scope>test</scope>
        </dependency>
    </dependencies>

    <dependencyManagement>
        <dependencies>
            <!-- MyBatis Plus BOM -->
            <dependency>
                <groupId>com.baomidou</groupId>
                <artifactId>mybatis-plus-bom</artifactId>
                <version>3.5.9</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>

            <!-- Spring Boot Dependencies -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <build>
        <plugins>
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-compiler-plugin</artifactId>
                <version>3.8.1</version>
                <configuration>
                    <source>1.8</source>
                    <target>1.8</target>
                    <encoding>UTF-8</encoding>
                </configuration>
            </plugin>
            <plugin>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-maven-plugin</artifactId>
                <version>${spring-boot.version}</version>
                <configuration>
                    <mainClass>com.yupi.yunshoppingbackend.YunShoppingBackendApplication</mainClass>
                </configuration>
                <executions>
                    <execution>
                        <id>repackage</id>
                        <goals>
                            <goal>repackage</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>
</project>
```

### 4.3 application.yml 配置

```yaml
server:
  port: 8124
  servlet:
    context-path: /api
    session:
      cookie:
        max-age: 2592000  # 30天

spring:
  application:
    name: yunshopping-backend

  # 数据库配置
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/yunshopping?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: your_password

  # Redis配置
  redis:
    database: 1
    host: 127.0.0.1
    port: 6379
    timeout: 5000
    password:

  # Session配置
  session:
    store-type: redis
    timeout: 2592000  # 30天

  # 文件上传配置
  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 50MB

  # RabbitMQ配置
  rabbitmq:
    host: 127.0.0.1
    port: 5672
    username: guest
    password: guest
    virtual-host: /

# MyBatis Plus配置
mybatis-plus:
  configuration:
    map-underscore-to-camel-case: false
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  global-config:
    db-config:
      logic-delete-field: isDelete
      logic-delete-value: 1
      logic-not-delete-value: 0

# Knife4j接口文档配置
knife4j:
  enable: true
  openapi:
    title: 云商城系统接口文档
    version: 1.0
    group:
      default:
        api-rule: package
        api-rule-resources:
          - com.yupi.yunshoppingbackend.controller

# 腾讯云COS配置（从环境变量获取）
cos:
  client:
    host: ${COS_HOST}
    secretId: ${COS_SECRET_ID}
    secretKey: ${COS_SECRET_KEY}
    region: ${COS_REGION}
    bucket: ${COS_BUCKET}

# 支付宝配置
alipay:
  appId: ${ALIPAY_APP_ID}
  privateKey: ${ALIPAY_PRIVATE_KEY}
  publicKey: ${ALIPAY_PUBLIC_KEY}
  gatewayUrl: https://openapi.alipay.com/gateway.do
  notifyUrl: ${ALIPAY_NOTIFY_URL}
  returnUrl: ${ALIPAY_RETURN_URL}

# 微信支付配置
wxpay:
  appId: ${WXPAY_APP_ID}
  mchId: ${WXPAY_MCH_ID}
  apiV3Key: ${WXPAY_API_V3_KEY}
  privateKeyPath: ${WXPAY_PRIVATE_KEY_PATH}
  merchantSerialNumber: ${WXPAY_MERCHANT_SERIAL_NUMBER}
  notifyUrl: ${WXPAY_NOTIFY_URL}
```

### 4.4 核心业务Service设计

#### 4.4.1 商品服务 (ProductService)

```java
public interface ProductService extends IService<Product> {

    /**
     * 创建商品
     */
    Long addProduct(ProductAddRequest request, User loginUser);

    /**
     * 编辑商品
     */
    Boolean editProduct(ProductEditRequest request, User loginUser);

    /**
     * 更新商品（管理员）
     */
    Boolean updateProduct(ProductUpdateRequest request);

    /**
     * 商品审核
     */
    Boolean reviewProduct(ProductReviewRequest request, User loginUser);

    /**
     * 分页查询商品
     */
    Page<ProductVO> listProductVOByPage(ProductQueryRequest request);

    /**
     * 商品上架/下架
     */
    Boolean updateProductStatus(Long productId, Integer status, User loginUser);

    /**
     * 扣减库存（订单创建时调用）
     */
    Boolean deductStock(Long productId, Integer quantity);

    /**
     * 恢复库存（订单取消时调用）
     */
    Boolean restoreStock(Long productId, Integer quantity);

    /**
     * 增加销量
     */
    Boolean increaseSales(Long productId, Integer quantity);

    /**
     * 搜索商品
     */
    Page<ProductVO> searchProduct(String keyword, ProductQueryRequest request);
}
```

#### 4.4.2 订单服务 (OrderService)

```java
public interface OrderService extends IService<Order> {

    /**
     * 创建订单
     */
    String createOrder(OrderCreateRequest request, User loginUser);

    /**
     * 取消订单
     */
    Boolean cancelOrder(Long orderId, User loginUser);

    /**
     * 支付订单
     */
    Boolean payOrder(Long orderId, String paymentMethod, User loginUser);

    /**
     * 确认收货
     */
    Boolean confirmReceive(Long orderId, User loginUser);

    /**
     * 发货（卖家操作）
     */
    Boolean shipOrder(ShipOrderRequest request, User loginUser);

    /**
     * 查询我的订单
     */
    Page<OrderVO> listMyOrders(OrderQueryRequest request, User loginUser);

    /**
     * 查询店铺订单（卖家）
     */
    Page<OrderVO> listShopOrders(OrderQueryRequest request, User loginUser);

    /**
     * 订单详情
     */
    OrderVO getOrderDetail(Long orderId, User loginUser);

    /**
     * 订单超时自动取消（定时任务）
     */
    void autoCancelTimeoutOrders();

    /**
     * 订单自动完成（定时任务）
     */
    void autoCompleteOrders();
}
```

#### 4.4.3 购物车服务 (CartService)

```java
public interface CartService extends IService<Cart> {

    /**
     * 添加到购物车
     */
    Boolean addToCart(CartAddRequest request, User loginUser);

    /**
     * 更新购物车商品数量
     */
    Boolean updateCartQuantity(Long cartId, Integer quantity, User loginUser);

    /**
     * 删除购物车商品
     */
    Boolean removeFromCart(Long cartId, User loginUser);

    /**
     * 批量删除购物车商品
     */
    Boolean batchRemove(List<Long> cartIds, User loginUser);

    /**
     * 查询我的购物车
     */
    List<CartVO> listMyCart(User loginUser);

    /**
     * 选中/取消选中
     */
    Boolean updateChecked(Long cartId, Boolean isChecked, User loginUser);

    /**
     * 全选/全不选
     */
    Boolean checkAll(Boolean isChecked, User loginUser);

    /**
     * 清空购物车
     */
    Boolean clearCart(User loginUser);
}
```

#### 4.4.4 支付服务 (PaymentService)

```java
public interface PaymentService extends IService<Payment> {

    /**
     * 创建支付宝支付
     */
    String createAlipay(Long orderId, User loginUser);

    /**
     * 创建微信支付
     */
    Map<String, String> createWxPay(Long orderId, User loginUser);

    /**
     * 支付宝回调处理
     */
    Boolean alipayNotify(Map<String, String> params);

    /**
     * 微信支付回调处理
     */
    Boolean wxpayNotify(String requestBody);

    /**
     * 查询支付状态
     */
    PaymentVO queryPaymentStatus(Long orderId);

    /**
     * 申请退款
     */
    Boolean refund(Long orderId, User loginUser);
}
```

### 4.5 权限控制设计

复用图片项目的权限控制机制：

```java
// 用户角色枚举
public enum UserRoleEnum {
    BUYER("buyer", "买家"),
    SELLER("seller", "卖家"),
    ADMIN("admin", "管理员");

    // ...
}

// 权限校验注解使用示例
@RestController
@RequestMapping("/product")
public class ProductController {

    // 卖家和管理员可以发布商品
    @PostMapping("/add")
    @AuthCheck(mustRole = "seller")
    public BaseResponse<Long> addProduct(@RequestBody ProductAddRequest request) {
        // ...
    }

    // 只有管理员可以审核商品
    @PostMapping("/review")
    @AuthCheck(mustRole = "admin")
    public BaseResponse<Boolean> reviewProduct(@RequestBody ProductReviewRequest request) {
        // ...
    }
}
```

---

## 第四部分：前端架构设计

### 5.1 项目目录结构

```
yunshopping-frontend/
├── src/
│   ├── api/                          # API接口层（OpenAPI自动生成）
│   │   ├── index.ts
│   │   ├── typings.d.ts              # API类型定义
│   │   ├── mainController.ts
│   │   ├── userController.ts
│   │   ├── addressController.ts      # 新增
│   │   ├── shopController.ts         # 新增
│   │   ├── categoryController.ts     # 新增
│   │   ├── productController.ts      # 新增
│   │   ├── cartController.ts         # 新增
│   │   ├── orderController.ts        # 新增
│   │   ├── paymentController.ts      # 新增
│   │   ├── reviewController.ts       # 新增
│   │   ├── couponController.ts       # 新增
│   │   └── fileController.ts
│   ├── components/                   # 可复用组件
│   │   ├── GlobalHeader.vue          # 全局顶部导航
│   │   ├── GlobalFooter.vue          # 全局底部
│   │   ├── GlobalSider.vue           # 全局侧边栏
│   │   ├── ProductCard.vue           # 商品卡片（新增）
│   │   ├── ProductList.vue           # 商品列表（新增）
│   │   ├── CartItem.vue              # 购物车商品项（新增）
│   │   ├── OrderCard.vue             # 订单卡片（新增）
│   │   ├── AddressSelect.vue         # 地址选择器（新增）
│   │   ├── ImageUpload.vue           # 图片上传
│   │   ├── CategoryTree.vue          # 分类树（新增）
│   │   └── ReviewList.vue            # 评价列表（新增）
│   ├── pages/                        # 页面组件
│   │   ├── HomePage.vue              # 首页（商品展示）
│   │   ├── ProductDetailPage.vue     # 商品详情页（新增）
│   │   ├── CartPage.vue              # 购物车页（新增）
│   │   ├── OrderConfirmPage.vue      # 确认订单页（新增）
│   │   ├── PaymentPage.vue           # 支付页（新增）
│   │   ├── OrderListPage.vue         # 订单列表页（新增）
│   │   ├── OrderDetailPage.vue       # 订单详情页（新增）
│   │   ├── MyShopPage.vue            # 我的店铺（新增）
│   │   ├── OpenShopPage.vue          # 开店申请页（新增）
│   │   ├── AddProductPage.vue        # 发布商品页（新增）
│   │   ├── ShopProductManagePage.vue # 店铺商品管理（新增）
│   │   ├── ShopOrderManagePage.vue   # 店铺订单管理（新增）
│   │   ├── admin/                    # 管理后台页面
│   │   │   ├── UserManagePage.vue
│   │   │   ├── ShopManagePage.vue    # 店铺审核管理（新增）
│   │   │   ├── ProductManagePage.vue # 商品审核管理（新增）
│   │   │   ├── OrderManagePage.vue   # 订单管理（新增）
│   │   │   └── CategoryManagePage.vue # 分类管理（新增）
│   │   └── user/                     # 用户相关页面
│   │       ├── UserLoginPage.vue
│   │       ├── UserRegisterPage.vue
│   │       ├── UserProfilePage.vue
│   │       ├── AddressManagePage.vue # 地址管理（新增）
│   │       └── MyFavoritePage.vue    # 我的收藏（新增）
│   ├── layouts/
│   │   └── BasicLayout.vue           # 基础布局
│   ├── router/
│   │   └── index.ts                  # 路由配置（30+路由）
│   ├── stores/                       # Pinia状态管理
│   │   ├── useLoginUserStore.ts      # 登录用户
│   │   ├── useCartStore.ts           # 购物车状态（新增）
│   │   └── useShopStore.ts           # 店铺状态（新增）
│   ├── constants/                    # 常量定义
│   │   ├── order.ts                  # 订单常量（新增）
│   │   ├── product.ts                # 商品常量（新增）
│   │   └── shop.ts                   # 店铺常量（新增）
│   ├── utils/                        # 工具函数
│   │   ├── index.ts
│   │   ├── price.ts                  # 价格格式化（新增）
│   │   └── order.ts                  # 订单工具（新增）
│   ├── request.ts                    # Axios请求配置
│   ├── access.ts                     # 权限控制
│   ├── App.vue
│   ├── main.ts
│   └── assets/
├── public/
├── package.json
├── tsconfig.json
├── vite.config.ts
├── openapi.config.js
└── index.html
```

### 5.2 package.json 配置

```json
{
  "name": "yunshopping-frontend",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "openapi": "node openapi.config.js",
    "build": "run-p type-check \"build-only {@}\" --",
    "preview": "vite preview",
    "build-only": "vite build",
    "type-check": "vue-tsc --build",
    "lint": "eslint . --fix",
    "format": "prettier --write src/"
  },
  "dependencies": {
    "ant-design-vue": "^4.2.6",
    "axios": "^1.12.2",
    "file-saver": "^2.0.5",
    "pinia": "^2.2.6",
    "tslib": "^2.8.1",
    "vue": "^3.5.13",
    "vue-cropper": "^1.1.4",
    "vue-router": "^4.4.5"
  },
  "devDependencies": {
    "@tsconfig/node22": "^22.0.0",
    "@types/file-saver": "^2.0.7",
    "@types/node": "^22.9.3",
    "@umijs/openapi": "^1.14.1",
    "@vitejs/plugin-vue": "^5.2.1",
    "@vue/eslint-config-prettier": "^10.1.0",
    "@vue/eslint-config-typescript": "^14.1.3",
    "@vue/tsconfig": "^0.7.0",
    "eslint": "^9.14.0",
    "eslint-plugin-vue": "^9.30.0",
    "npm-run-all2": "^7.0.1",
    "prettier": "^3.3.3",
    "typescript": "~5.6.3",
    "vite": "^6.0.1",
    "vite-plugin-vue-devtools": "^7.6.5",
    "vue-tsc": "^2.1.10"
  }
}
```

### 5.3 路由设计

```typescript
// router/index.ts
const routes = [
  // 公共路由
  { path: '/', component: HomePage },
  { path: '/product/:id', component: ProductDetailPage },
  { path: '/cart', component: CartPage },

  // 用户路由
  { path: '/user/login', component: UserLoginPage },
  { path: '/user/register', component: UserRegisterPage },
  { path: '/user/profile', component: UserProfilePage },
  { path: '/user/address', component: AddressManagePage },
  { path: '/user/favorite', component: MyFavoritePage },

  // 订单路由
  { path: '/order/confirm', component: OrderConfirmPage },
  { path: '/order/payment/:orderId', component: PaymentPage },
  { path: '/order/list', component: OrderListPage },
  { path: '/order/detail/:orderId', component: OrderDetailPage },

  // 卖家路由（需要seller权限）
  { path: '/shop/open', component: OpenShopPage },
  { path: '/shop/my', component: MyShopPage },
  { path: '/shop/product/add', component: AddProductPage },
  { path: '/shop/product/manage', component: ShopProductManagePage },
  { path: '/shop/order/manage', component: ShopOrderManagePage },

  // 管理员路由（需要admin权限）
  { path: '/admin/user', component: UserManagePage },
  { path: '/admin/shop', component: ShopManagePage },
  { path: '/admin/product', component: ProductManagePage },
  { path: '/admin/order', component: OrderManagePage },
  { path: '/admin/category', component: CategoryManagePage },
];
```

### 5.4 状态管理设计

#### 5.4.1 购物车Store

```typescript
// stores/useCartStore.ts
export const useCartStore = defineStore('cart', () => {
  const cartItems = ref<API.CartVO[]>([]);
  const cartCount = computed(() => cartItems.value.length);
  const totalPrice = computed(() => {
    return cartItems.value
      .filter(item => item.isChecked)
      .reduce((sum, item) => sum + item.price * item.quantity, 0);
  });

  // 获取购物车列表
  const fetchCart = async () => {
    const res = await listMyCartUsingGet();
    if (res.code === 0) {
      cartItems.value = res.data || [];
    }
  };

  // 添加到购物车
  const addToCart = async (productId: number, quantity: number) => {
    const res = await addToCartUsingPost({ productId, quantity });
    if (res.code === 0) {
      await fetchCart();
      return true;
    }
    return false;
  };

  // 更新数量
  const updateQuantity = async (cartId: number, quantity: number) => {
    // ...
  };

  // 删除购物车商品
  const removeItem = async (cartId: number) => {
    // ...
  };

  return {
    cartItems,
    cartCount,
    totalPrice,
    fetchCart,
    addToCart,
    updateQuantity,
    removeItem,
  };
});
```

---

## 第五部分：API接口设计

### 6.1 接口设计规范

**统一响应格式**：
```json
{
  "code": 0,
  "data": {},
  "message": "success"
}
```

**错误码设计**：
```java
SUCCESS(0, "成功"),
PARAMS_ERROR(40000, "参数错误"),
NOT_LOGIN_ERROR(40100, "未登录"),
NO_AUTH_ERROR(40101, "无权限"),
NOT_FOUND_ERROR(40400, "数据不存在"),
FORBIDDEN_ERROR(40300, "禁止访问"),
SYSTEM_ERROR(50000, "系统错误"),
OPERATION_ERROR(50001, "操作失败"),

// 业务错误码（新增）
INSUFFICIENT_STOCK(50100, "库存不足"),
ORDER_NOT_PAID(50101, "订单未支付"),
ORDER_EXPIRED(50102, "订单已过期"),
PAYMENT_FAILED(50103, "支付失败"),
SHOP_NOT_APPROVED(50104, "店铺未通过审核"),
```

### 6.2 核心API接口列表

#### 6.2.1 用户模块

| 接口 | 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|------|
| 用户注册 | POST | /user/register | 公开 | 买家注册 |
| 用户登录 | POST | /user/login | 公开 | 登录 |
| 获取当前用户 | GET | /user/get/login | 登录 | 获取登录用户信息 |
| 用户登出 | POST | /user/logout | 登录 | 登出 |
| 更新用户信息 | POST | /user/update | 登录 | 更新个人信息 |

#### 6.2.2 地址模块

| 接口 | 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|------|
| 添加地址 | POST | /address/add | 登录 | 添加收货地址 |
| 编辑地址 | POST | /address/edit | 登录 | 编辑地址 |
| 删除地址 | POST | /address/delete | 登录 | 删除地址 |
| 我的地址列表 | GET | /address/list/my | 登录 | 查询我的地址 |
| 设置默认地址 | POST | /address/setDefault | 登录 | 设置默认地址 |

#### 6.2.3 店铺模块

| 接口 | 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|------|
| 申请开店 | POST | /shop/apply | 登录 | 申请开店 |
| 编辑店铺 | POST | /shop/edit | seller | 编辑店铺信息 |
| 获取我的店铺 | GET | /shop/my | seller | 获取我的店铺 |
| 获取店铺详情 | GET | /shop/get/{id} | 公开 | 获取店铺详情 |
| 店铺审核 | POST | /shop/review | admin | 审核店铺 |
| 店铺列表 | POST | /shop/list/page | admin | 分页查询店铺 |

#### 6.2.4 商品模块

| 接口 | 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|------|
| 发布商品 | POST | /product/add | seller | 发布商品 |
| 编辑商品 | POST | /product/edit | seller | 编辑商品 |
| 删除商品 | POST | /product/delete | seller | 删除商品 |
| 商品上下架 | POST | /product/updateStatus | seller | 上下架 |
| 商品详情 | GET | /product/get/{id} | 公开 | 商品详情 |
| 商品列表 | POST | /product/list/page | 公开 | 分页查询商品 |
| 搜索商品 | POST | /product/search | 公开 | 搜索商品 |
| 我的商品 | POST | /product/list/my | seller | 查询我的商品 |
| 商品审核 | POST | /product/review | admin | 审核商品 |

#### 6.2.5 购物车模块

| 接口 | 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|------|
| 添加到购物车 | POST | /cart/add | 登录 | 加入购物车 |
| 更新数量 | POST | /cart/updateQuantity | 登录 | 更新数量 |
| 删除商品 | POST | /cart/remove | 登录 | 删除 |
| 批量删除 | POST | /cart/batchRemove | 登录 | 批量删除 |
| 我的购物车 | GET | /cart/list/my | 登录 | 查询购物车 |
| 选中/取消选中 | POST | /cart/updateChecked | 登录 | 更新选中状态 |
| 清空购物车 | POST | /cart/clear | 登录 | 清空购物车 |

#### 6.2.6 订单模块

| 接口 | 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|------|
| 创建订单 | POST | /order/create | 登录 | 创建订单 |
| 取消订单 | POST | /order/cancel | 登录 | 取消订单 |
| 确认收货 | POST | /order/confirm | 登录 | 确认收货 |
| 订单详情 | GET | /order/get/{id} | 登录 | 订单详情 |
| 我的订单 | POST | /order/list/my | 登录 | 买家订单列表 |
| 店铺订单 | POST | /order/list/shop | seller | 卖家订单列表 |
| 发货 | POST | /order/ship | seller | 卖家发货 |
| 订单管理 | POST | /order/list/page | admin | 管理员查询 |

#### 6.2.7 支付模块

| 接口 | 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|------|
| 创建支付宝支付 | POST | /payment/alipay/create | 登录 | 支付宝支付 |
| 创建微信支付 | POST | /payment/wxpay/create | 登录 | 微信支付 |
| 支付宝回调 | POST | /payment/alipay/notify | 公开 | 支付宝异步通知 |
| 微信回调 | POST | /payment/wxpay/notify | 公开 | 微信异步通知 |
| 查询支付状态 | GET | /payment/query/{orderId} | 登录 | 查询支付状态 |
| 申请退款 | POST | /payment/refund | 登录 | 申请退款 |

#### 6.2.8 评价模块

| 接口 | 方法 | 路径 | 权限 | 说明 |
|------|------|------|------|------|
| 添加评价 | POST | /review/add | 登录 | 评价商品 |
| 商家回复 | POST | /review/reply | seller | 商家回复评价 |
| 商品评价列表 | POST | /review/list/product | 公开 | 查询商品评价 |
| 我的评价 | POST | /review/list/my | 登录 | 我的评价 |

---

## 第六部分：核心业务流程设计

### 7.1 用户注册登录流程

```
用户注册（买家）
  ↓
填写账号、密码、手机号
  ↓
后端校验：账号唯一性、密码强度
  ↓
密码加密存储（MD5 + 盐）
  ↓
插入user表（userRole=buyer）
  ↓
注册成功

用户登录
  ↓
输入账号密码
  ↓
后端校验密码
  ↓
生成Session存储到Redis
  ↓
返回用户信息（脱敏）
  ↓
前端存储到Pinia Store
```

### 7.2 店铺开通流程

```
用户点击"我要开店"
  ↓
填写店铺信息（店铺名、Logo、描述等）
  ↓
提交开店申请
  ↓
后端创建shop记录（shopStatus=0待审核，reviewStatus=0待审核）
  ↓
管理员在后台审核
  ↓
审核通过 → 更新reviewStatus=1，shopStatus=1（营业中）
           更新用户角色为seller
  ↓
审核拒绝 → 更新reviewStatus=2，填写拒绝原因
  ↓
卖家收到审核结果通知
```

### 7.3 商品发布流程

```
卖家登录 → 进入"我的店铺"
  ↓
点击"发布商品"
  ↓
填写商品信息
  ├── 基本信息：商品名称、标题、描述
  ├── 分类选择：选择商品分类
  ├── 价格库存：价格、库存、单位
  ├── 商品图片：上传到COS（主图+详情图）
  └── 规格信息：规格（JSON格式）
  ↓
提交发布
  ↓
后端创建product记录
  ├── status=0（待上架）
  ├── reviewStatus=0（待审核）
  └── 上传图片到腾讯云COS
  ↓
管理员审核商品
  ↓
审核通过 → reviewStatus=1
  ↓
卖家手动上架 → status=1（已上架）
  ↓
商品在前台展示
```

### 7.4 购物流程

```
买家浏览商品
  ↓
点击"加入购物车"
  ↓
后端插入cart表（或更新数量）
  ↓
前端购物车角标+1
  ↓
买家进入购物车页面
  ↓
选择商品、修改数量
  ↓
点击"去结算"
  ↓
跳转到确认订单页
  ├── 选择收货地址
  ├── 填写买家留言
  ├── 选择优惠券（如果有）
  └── 显示订单金额
  ↓
点击"提交订单"
```

### 7.5 订单创建流程

```
点击"提交订单"
  ↓
后端处理
  ├── 1. 校验商品库存
  ├── 2. 计算订单金额
  │      ├── 商品总价
  │      ├── 优惠券优惠
  │      ├── 运费
  │      └── 实付金额
  ├── 3. 生成订单号（时间戳+随机数）
  ├── 4. 扣减商品库存（乐观锁）
  ├── 5. 插入orders表
  ├── 6. 插入order_item表（订单详情）
  ├── 7. 使用优惠券（更新user_coupon状态）
  └── 8. 清空购物车中已结算商品
  ↓
订单创建成功
  ↓
跳转到支付页面
```

### 7.6 支付流程（支付宝）

```
订单创建成功 → 跳转支付页
  ↓
选择支付宝支付
  ↓
调用后端 /payment/alipay/create
  ↓
后端调用支付宝SDK
  ├── 生成支付流水号
  ├── 创建payment记录（payStatus=0待支付）
  └── 返回支付宝支付表单HTML
  ↓
前端渲染支付表单 → 跳转支付宝
  ↓
用户在支付宝完成支付
  ↓
支付宝异步回调 /payment/alipay/notify
  ↓
后端处理回调
  ├── 1. 验证签名
  ├── 2. 更新payment表（payStatus=1已支付）
  ├── 3. 更新orders表（orderStatus=1已支付，paymentStatus=1）
  ├── 4. 记录支付时间
  └── 5. 发送MQ消息（可选）
  ↓
支付完成
  ↓
前端轮询或WebSocket获取支付结果
  ↓
跳转到"支付成功"页面
```

### 7.7 订单发货流程

```
卖家登录 → 进入"店铺订单管理"
  ↓
查看待发货订单（orderStatus=2待发货）
  ↓
点击"发货"
  ↓
填写物流信息
  ├── 物流公司
  └── 物流单号
  ↓
提交发货
  ↓
后端处理
  ├── 1. 更新orders表（orderStatus=3已发货，shippingStatus=1）
  ├── 2. 插入logistics表
  └── 3. 记录发货时间
  ↓
买家收到发货通知
  ↓
买家可查看物流信息
```

### 7.8 订单完成流程

```
买家收到货物
  ↓
点击"确认收货"
  ↓
后端处理
  ├── 1. 更新orders表（orderStatus=4已完成，shippingStatus=2已签收）
  ├── 2. 更新logistics表（logisticsStatus=3已签收）
  ├── 3. 记录完成时间
  ├── 4. 增加商品销量
  └── 5. 更新店铺总销量和总营收
  ↓
订单完成
  ↓
买家可以评价商品
```

### 7.9 商品评价流程

```
订单完成后
  ↓
买家点击"评价"
  ↓
填写评价内容
  ├── 评分（1-5星）
  ├── 评价文字
  ├── 上传图片（可选）
  └── 是否匿名
  ↓
提交评价
  ↓
后端插入review表
  ↓
更新商品评价数
  ↓
评价显示在商品详情页
  ↓
卖家可以回复评价
```

---

## 第七部分：文件存储方案

### 8.1 腾讯云COS使用场景

| 场景 | 存储内容 | 处理方式 |
|------|---------|---------|
| 商品主图 | 商品主图片 | 压缩、裁剪、转WebP |
| 商品详情图 | 商品详情图片 | 压缩、转WebP |
| 店铺Logo | 店铺Logo | 裁剪为正方形、压缩 |
| 店铺横幅 | 店铺Banner | 裁剪为16:9、压缩 |
| 用户头像 | 用户头像 | 裁剪为正方形、压缩 |
| 评价图片 | 评价晒图 | 压缩、转WebP |
| 轮播图 | 首页轮播图 | 裁剪、压缩 |

### 8.2 COS配置示例

```java
@Configuration
public class CosClientConfig {

    @Value("${cos.client.secretId}")
    private String secretId;

    @Value("${cos.client.secretKey}")
    private String secretKey;

    @Value("${cos.client.region}")
    private String region;

    @Value("${cos.client.bucket}")
    private String bucket;

    @Bean
    public COSClient cosClient() {
        COSCredentials cred = new BasicCOSCredentials(secretId, secretKey);
        Region regionObj = new Region(region);
        ClientConfig clientConfig = new ClientConfig(regionObj);
        return new COSClient(cred, clientConfig);
    }
}
```

### 8.3 图片上传流程

```
前端选择图片
  ↓
调用 /file/upload 接口
  ↓
后端接收MultipartFile
  ↓
CosManager处理
  ├── 1. 校验文件类型（jpg/png/webp）
  ├── 2. 校验文件大小（<5MB）
  ├── 3. 生成唯一文件名（UUID + 后缀）
  ├── 4. 上传到COS
  └── 5. 返回COS URL
  ↓
前端获取URL并显示
```

---

## 第八部分：部署方案

### 9.1 开发环境

```yaml
# 开发环境配置
运行环境:
  - JDK 1.8+
  - MySQL 8.0+
  - Redis 7.0+
  - RabbitMQ 3.12+
  - Node.js 18+
  - npm 10+

启动步骤:
  1. 启动MySQL并导入sql/create_table.sql
  2. 启动Redis
  3. 启动RabbitMQ
  4. 配置application-local.yml
  5. 启动后端：YunShoppingBackendApplication
  6. 安装前端依赖：npm install
  7. 启动前端：npm run dev
```

### 9.2 生产环境

```yaml
# 生产环境架构
负载均衡:
  - Nginx（反向代理、静态资源）

应用服务器:
  - 后端：Spring Boot × 2（8124端口）
  - 前端：Nginx（静态托管）

数据库:
  - MySQL主从（读写分离）
  - Redis集群（Session + 缓存）
  - RabbitMQ集群

文件存储:
  - 腾讯云COS

监控:
  - Prometheus + Grafana
  - ELK日志系统
```

---

## 第九部分：扩展功能设计

### 10.1 优惠券系统

**核心功能**：
- 满减券、折扣券、无门槛券
- 平台券和店铺券
- 领取、使用、过期机制
- 订单自动计算优惠

**关键表**：
- `coupon` - 优惠券
- `user_coupon` - 用户优惠券

### 10.2 秒杀系统

**核心功能**：
- 秒杀活动创建
- 库存预扣减（Redis）
- 限购机制
- 防超卖（Lua脚本）

**技术方案**：
```
秒杀开始前
  ↓
商品库存同步到Redis
  ↓
用户点击秒杀
  ↓
Redis Lua脚本扣减库存
  ↓
扣减成功 → 创建订单
  ↓
扣减失败 → 返回"已售罄"
  ↓
定时任务同步Redis库存到MySQL
```

### 10.3 拼团系统（可选）

**核心功能**：
- 创建拼团活动
- 发起拼团
- 参与拼团
- 成团/未成团处理

### 10.4 积分系统（可选）

**核心功能**：
- 购物获得积分
- 积分兑换优惠券
- 积分抵扣

---

## 第十部分：项目开发计划

### 11.1 开发阶段划分

**第一阶段：基础模块（2周）**
- 用户模块（注册、登录、个人信息）
- 地址模块（收货地址管理）
- 文件上传模块（COS集成）
- 权限控制（AOP + 注解）

**第二阶段：商品模块（2周）**
- 分类管理
- 商品发布、编辑、删除
- 商品审核
- 商品搜索和列表

**第三阶段：店铺模块（1周）**
- 店铺开通申请
- 店铺审核
- 店铺信息管理

**第四阶段：交易模块（3周）**
- 购物车
- 订单创建
- 支付集成（支付宝、微信）
- 订单状态管理
- 物流跟踪

**第五阶段：评价模块（1周）**
- 商品评价
- 商家回复
- 评价展示

**第六阶段：营销模块（2周）**
- 优惠券系统
- 秒杀活动

**第七阶段：测试优化（1周）**
- 单元测试
- 接口测试
- 性能优化
- Bug修复

### 11.2 技术难点

| 难点 | 解决方案 |
|------|---------|
| 订单并发 | Redis分布式锁 + 数据库乐观锁 |
| 库存超卖 | Redis预扣减 + Lua脚本 |
| 支付回调幂等性 | 支付流水号 + 状态机 |
| 订单超时取消 | RabbitMQ延迟队列 |
| 商品搜索性能 | MySQL索引优化 + Redis缓存 |
| 图片加载慢 | COS图片处理 + CDN加速 |

---

## 总结

本技术方案完整设计了云商城系统的前后端架构、数据库、API接口、业务流程和部署方案。

**核心特点**：
1. 完全复用图片项目的技术栈和架构设计
2. 支持多店铺B2C模式
3. 完整的交易流程（购物车→订单→支付→物流→评价）
4. 丰富的营销功能（优惠券、秒杀）
5. 使用腾讯云COS存储商品图片
6. 完善的权限控制和审核流程

**项目规模**：
- 后端：15个数据库表、13个Controller、15个Service
- 前端：30+页面、50+组件
- 预计开发周期：12周（3个月）

---

**文档版本**：v1.0.0
**编写日期**：2026-01-02
**参考项目**：yu-picture-backend / yu-picture-frontend
