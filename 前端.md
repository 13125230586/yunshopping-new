# 云商城系统前端对接技术文档

## 文档信息

| 文档属性 | 信息 |
|---------|------|
| 项目名称 | 云商城系统 (YunShopping Mall System) |
| 后端项目 | yunshopping-backend |
| 文档版本 | v1.0.0 |
| 更新日期 | 2026-01-02 |
| 文档类型 | 前端对接技术文档 |

---

## 一、项目概述

### 1.1 技术架构

**后端技术栈**：
- Spring Boot 2.7.6 + MyBatis-Plus 3.5.9
- MySQL 8.0 + Redis 7.0
- 腾讯云 COS 对象存储
- Session 认证机制

**前端技术栈**（推荐）：
- Vue 3.5.13 + TypeScript 5.6.3
- Vite 6.0.1
- Ant Design Vue 4.2.6
- Axios 1.12.2
- Pinia 2.2.6

### 1.2 接口规范

**基础URL**：
```
开发环境：http://localhost:8101
生产环境：https://your-domain.com
```

**统一响应格式**：
```typescript
interface BaseResponse<T> {
  code: number;        // 状态码：0-成功，其他-失败
  data: T;             // 响应数据
  message: string;     // 响应消息
}
```

**常见状态码**：
```typescript
const ErrorCode = {
  SUCCESS: 0,                    // 成功
  PARAMS_ERROR: 40000,           // 请求参数错误
  NOT_LOGIN_ERROR: 40100,        // 未登录
  NO_AUTH_ERROR: 40101,          // 无权限
  NOT_FOUND_ERROR: 40400,        // 请求数据不存在
  FORBIDDEN_ERROR: 40300,        // 禁止访问
  SYSTEM_ERROR: 50000,           // 系统内部异常
  OPERATION_ERROR: 50001,        // 操作失败
}
```

**分页请求格式**：
```typescript
interface PageRequest {
  current?: number;    // 当前页码，默认 1
  pageSize?: number;   // 每页数量，默认 10
  sortField?: string;  // 排序字段
  sortOrder?: string;  // 排序顺序："ascend" | "descend"
}
```

**分页响应格式**：
```typescript
interface Page<T> {
  records: T[];        // 数据列表
  total: number;       // 总记录数
  size: number;        // 每页数量
  current: number;     // 当前页码
  pages: number;       // 总页数
}
```

---

## 二、用户模块 API

### 2.1 用户注册

**接口地址**：`POST /user/register`

**请求参数**：
```typescript
interface UserRegisterRequest {
  userAccount: string;      // 用户账号，4-16位
  userPassword: string;     // 用户密码，8-16位
  checkPassword: string;    // 确认密码
}
```

**响应数据**：
```typescript
BaseResponse<number>  // 返回新用户的 ID
```

**调用示例**：
```typescript
const register = async (data: UserRegisterRequest) => {
  const response = await axios.post<BaseResponse<number>>('/user/register', data);
  return response.data;
};
```

---

### 2.2 用户登录

**接口地址**：`POST /user/login`

**请求参数**：
```typescript
interface UserLoginRequest {
  userAccount: string;      // 用户账号
  userPassword: string;     // 用户密码
}
```

**响应数据**：
```typescript
interface User {
  id: number;
  userAccount: string;
  userName: string;
  userAvatar: string;
  userProfile: string;
  userRole: 'buyer' | 'seller' | 'admin';
  phone: string;
  email: string;
  gender: 0 | 1 | 2;        // 0-未知 1-男 2-女
  birthday: string;
  defaultAddressId: number;
  createTime: string;
}

BaseResponse<User>
```

**调用示例**：
```typescript
const login = async (data: UserLoginRequest) => {
  const response = await axios.post<BaseResponse<User>>('/user/login', data);
  return response.data;
};
```

---

### 2.3 获取当前登录用户

**接口地址**：`GET /user/get/login`

**请求参数**：无

**响应数据**：
```typescript
BaseResponse<User>
```

---

### 2.4 用户注销

**接口地址**：`POST /user/logout`

**请求参数**：无

**响应数据**：
```typescript
BaseResponse<boolean>  // true-注销成功
```

---

## 三、商品模块 API

### 3.1 添加商品

**接口地址**：`POST /product/add`

**权限要求**：需要登录

**请求参数**：
```typescript
interface ProductAddRequest {
  productName: string;          // 商品名称
  productTitle: string;         // 商品标题
  productDescription: string;   // 商品描述
  categoryId: number;           // 分类ID
  shopId: number;               // 店铺ID
  brandName: string;            // 品牌名称
  mainImageUrl: string;         // 主图URL
  imageUrls: string;            // 图片URL列表（JSON数组字符串）
  price: number;                // 价格
  originalPrice: number;        // 原价
  stock: number;                // 库存
  unit: string;                 // 计量单位
  weight: number;               // 重量（kg）
  tags: string;                 // 标签（JSON数组字符串）
  specifications: string;       // 规格（JSON字符串）
}
```

**响应数据**：
```typescript
BaseResponse<number>  // 返回商品 ID
```

---

### 3.2 商品审核（管理员）

**接口地址**：`POST /product/review`

**权限要求**：admin

**请求参数**：
```typescript
interface ProductReviewRequest {
  id: number;                   // 商品ID
  reviewStatus: 0 | 1 | 2;      // 审核状态：0-待审核 1-通过 2-拒绝
  reviewMessage: string;        // 审核备注
}
```

**响应数据**：
```typescript
BaseResponse<boolean>
```

---

### 3.3 删除商品

**接口地址**：`POST /product/delete`

**权限要求**：需要登录，仅商品所有者或管理员可删除

**请求参数**：
```typescript
interface DeleteRequest {
  id: number;  // 商品ID
}
```

**响应数据**：
```typescript
BaseResponse<boolean>
```

---

### 3.4 分页查询商品

**接口地址**：`POST /product/list/page`

**请求参数**：
```typescript
interface ProductQueryRequest extends PageRequest {
  id?: number;               // 商品ID
  productName?: string;      // 商品名称（模糊搜索）
  categoryId?: number;       // 分类ID
  shopId?: number;           // 店铺ID
  brandName?: string;        // 品牌名称
  minPrice?: number;         // 最低价格
  maxPrice?: number;         // 最高价格
  status?: 0 | 1 | 2;        // 商品状态：0-待上架 1-已上架 2-已下架
  reviewStatus?: 0 | 1 | 2;  // 审核状态：0-待审核 1-通过 2-拒绝
  searchText?: string;       // 搜索文本（搜索商品名称和描述）
}
```

**响应数据**：
```typescript
interface ProductVO {
  id: number;
  productName: string;
  productTitle: string;
  productDescription: string;
  categoryId: number;
  shopId: number;
  shopName: string;
  brandName: string;
  mainImageUrl: string;
  imageUrls: string;           // JSON数组字符串
  price: number;
  originalPrice: number;
  stock: number;
  sales: number;
  unit: string;
  weight: number;
  tags: string;                // JSON数组字符串
  specifications: string;      // JSON字符串
  status: 0 | 1 | 2;
  viewCount: number;
  favoriteCount: number;
  createTime: string;
}

BaseResponse<Page<ProductVO>>
```

---

### 3.5 获取商品详情

**接口地址**：`GET /product/get`

**请求参数**：
```typescript
{
  id: number  // 商品ID（Query参数）
}
```

**响应数据**：
```typescript
BaseResponse<ProductVO>
```

---

## 四、分类模块 API

### 4.1 获取分类树

**接口地址**：`GET /category/tree`

**请求参数**：无

**响应数据**：
```typescript
interface CategoryVO {
  id: number;
  categoryName: string;
  parentId: number;
  level: 1 | 2 | 3;           // 分类级别
  sortOrder: number;
  icon: string;
  children: CategoryVO[];     // 子分类列表
}

BaseResponse<CategoryVO[]>  // 返回树形结构的分类列表
```

---

### 4.2 添加分类（管理员）

**接口地址**：`POST /category/add`

**权限要求**：admin

**请求参数**：
```typescript
interface Category {
  categoryName: string;
  parentId: number;           // 父分类ID，0表示一级分类
  level: 1 | 2 | 3;
  sortOrder: number;
  icon: string;
}
```

**响应数据**：
```typescript
BaseResponse<number>  // 返回分类 ID
```

---

### 4.3 更新分类（管理员）

**接口地址**：`POST /category/update`

**权限要求**：admin

**请求参数**：
```typescript
interface Category {
  id: number;                 // 分类ID
  categoryName: string;
  parentId: number;
  level: 1 | 2 | 3;
  sortOrder: number;
  icon: string;
}
```

**响应数据**：
```typescript
BaseResponse<boolean>
```

---

### 4.4 删除分类（管理员）

**接口地址**：`POST /category/delete`

**权限要求**：admin

**请求参数**：
```typescript
interface DeleteRequest {
  id: number;  // 分类ID
}
```

**响应数据**：
```typescript
BaseResponse<boolean>
```

---

## 五、店铺模块 API

### 5.1 店铺入驻申请

**接口地址**：`POST /shop/add`

**权限要求**：需要登录

**请求参数**：
```typescript
interface ShopAddRequest {
  shopName: string;            // 店铺名称
  shopLogo: string;            // 店铺Logo URL
  shopBanner: string;          // 店铺横幅URL
  shopDescription: string;     // 店铺描述
  shopLevel: 0 | 1 | 2;        // 店铺等级：0-普通店 1-品牌店 2-旗舰店
  province: string;            // 省份
  city: string;                // 城市
}
```

**响应数据**：
```typescript
BaseResponse<number>  // 返回店铺 ID
```

---

### 5.2 店铺审核（管理员）

**接口地址**：`POST /shop/review`

**权限要求**：admin

**请求参数**：
```typescript
interface ShopReviewRequest {
  id: number;                  // 店铺ID
  reviewStatus: 0 | 1 | 2;     // 审核状态：0-待审核 1-通过 2-拒绝
  reviewMessage: string;       // 审核备注
}
```

**响应数据**：
```typescript
BaseResponse<boolean>
```

---

### 5.3 删除店铺（管理员）

**接口地址**：`POST /shop/delete`

**权限要求**：admin

**请求参数**：
```typescript
interface DeleteRequest {
  id: number;  // 店铺ID
}
```

**响应数据**：
```typescript
BaseResponse<boolean>
```

---

### 5.4 分页查询店铺

**接口地址**：`POST /shop/list/page`

**请求参数**：
```typescript
interface ShopQueryRequest extends PageRequest {
  id?: number;               // 店铺ID
  shopName?: string;         // 店铺名称（模糊搜索）
  shopLevel?: 0 | 1 | 2;     // 店铺等级
  shopStatus?: 0 | 1 | 2 | 3;// 店铺状态：0-待审核 1-营业中 2-已关闭 3-审核拒绝
  userId?: number;           // 店主用户ID
  reviewStatus?: 0 | 1 | 2;  // 审核状态
}
```

**响应数据**：
```typescript
interface ShopVO {
  id: number;
  shopName: string;
  shopLogo: string;
  shopBanner: string;
  shopDescription: string;
  shopLevel: 0 | 1 | 2;
  shopStatus: 0 | 1 | 2 | 3;
  userId: number;
  totalSales: number;
  totalRevenue: number;
  rating: number;             // 评分（1-5分）
  reviewCount: number;
  province: string;
  city: string;
  createTime: string;
}

BaseResponse<Page<ShopVO>>
```

---

### 5.5 获取店铺详情

**接口地址**：`GET /shop/get`

**请求参数**：
```typescript
{
  id: number  // 店铺ID（Query参数）
}
```

**响应数据**：
```typescript
BaseResponse<ShopVO>
```

---

## 六、购物车模块 API

### 6.1 添加到购物车

**接口地址**：`POST /cart/add`

**权限要求**：需要登录

**请求参数**：
```typescript
interface CartAddRequest {
  productId: number;        // 商品ID
  quantity: number;         // 数量
  specification: string;    // 规格（JSON字符串）
}
```

**响应数据**：
```typescript
BaseResponse<number>  // 返回购物车项 ID
```

---

### 6.2 获取购物车列表

**接口地址**：`GET /cart/list`

**权限要求**：需要登录

**请求参数**：无

**响应数据**：
```typescript
interface CartVO {
  id: number;
  productId: number;
  productName: string;
  productImage: string;
  price: number;
  quantity: number;
  specification: string;
  isChecked: 0 | 1;         // 是否勾选
  stock: number;            // 库存
  shopId: number;
  shopName: string;
}

BaseResponse<CartVO[]>
```

---

### 6.3 更新购物车商品数量

**接口地址**：`POST /cart/update`

**权限要求**：需要登录

**请求参数**：
```typescript
{
  cartId: number;           // 购物车项ID（RequestParam）
  quantity: number;         // 新数量（RequestParam）
}
```

**响应数据**：
```typescript
BaseResponse<boolean>
```

---

### 6.4 删除购物车商品

**接口地址**：`POST /cart/delete`

**权限要求**：需要登录

**请求参数**：
```typescript
interface DeleteRequest {
  id: number;  // 购物车项ID
}
```

**响应数据**：
```typescript
BaseResponse<boolean>
```

---

### 6.5 清空购物车

**接口地址**：`POST /cart/clear`

**权限要求**：需要登录

**请求参数**：无

**响应数据**：
```typescript
BaseResponse<boolean>
```

---

## 七、订单模块 API

### 7.1 创建订单

**接口地址**：`POST /order/create`

**权限要求**：需要登录

**请求参数**：
```typescript
interface OrderCreateRequest {
  shopId: number;              // 店铺ID
  cartIds: number[];           // 购物车ID列表
  addressId: number;           // 收货地址ID
  buyerMessage: string;        // 买家留言
  couponId: number;            // 优惠券ID
}
```

**响应数据**：
```typescript
BaseResponse<number>  // 返回订单 ID
```

---

### 7.2 取消订单

**接口地址**：`POST /order/cancel`

**权限要求**：需要登录

**请求参数**：
```typescript
{
  orderId: number  // 订单ID（RequestParam）
}
```

**响应数据**：
```typescript
BaseResponse<boolean>
```

---

### 7.3 确认收货

**接口地址**：`POST /order/confirm`

**权限要求**：需要登录

**请求参数**：
```typescript
{
  orderId: number  // 订单ID（RequestParam）
}
```

**响应数据**：
```typescript
BaseResponse<boolean>
```

---

### 7.4 删除订单

**接口地址**：`POST /order/delete`

**权限要求**：需要登录

**请求参数**：
```typescript
{
  orderId: number  // 订单ID（RequestParam）
}
```

**响应数据**：
```typescript
BaseResponse<boolean>
```

---

### 7.5 分页查询订单

**接口地址**：`POST /order/list/page`

**权限要求**：需要登录

**请求参数**：
```typescript
interface OrderQueryRequest extends PageRequest {
  id?: number;                 // 订单ID
  orderNo?: string;            // 订单号
  shopId?: number;             // 店铺ID
  orderStatus?: 0 | 1 | 2 | 3 | 4 | 5 | 6 | 7;  // 订单状态
  paymentStatus?: 0 | 1 | 2;   // 支付状态
}

// 订单状态说明：
// 0-待支付 1-已支付 2-待发货 3-已发货 4-已完成 5-已取消 6-退款中 7-已退款
```

**响应数据**：
```typescript
interface OrderItemVO {
  id: number;
  productId: number;
  productName: string;
  productImage: string;
  price: number;
  quantity: number;
  totalAmount: number;
  specification: string;
}

interface OrderVO {
  id: number;
  orderNo: string;
  shopId: number;
  shopName: string;
  totalAmount: number;
  payAmount: number;
  discountAmount: number;
  shippingFee: number;
  paymentMethod: string;
  orderStatus: 0 | 1 | 2 | 3 | 4 | 5 | 6 | 7;
  paymentStatus: 0 | 1 | 2;
  shippingStatus: 0 | 1 | 2;
  receiverName: string;
  receiverPhone: string;
  receiverAddress: string;
  buyerMessage: string;
  payTime: string;
  shipTime: string;
  completeTime: string;
  createTime: string;
  orderItems: OrderItemVO[];  // 订单详情列表
}

BaseResponse<Page<OrderVO>>
```

---

### 7.6 获取订单详情

**接口地址**：`GET /order/get`

**权限要求**：需要登录

**请求参数**：
```typescript
{
  orderId: number  // 订单ID（Query参数）
}
```

**响应数据**：
```typescript
BaseResponse<OrderVO>
```

---

## 八、支付模块 API

### 8.1 创建支付

**接口地址**：`POST /payment/create`

**权限要求**：需要登录

**请求参数**：
```typescript
{
  orderId: number;          // 订单ID（RequestParam）
  paymentMethod: 1 | 2;     // 支付方式：1-支付宝 2-微信（RequestParam）
}
```

**响应数据**：
```typescript
BaseResponse<number>  // 返回支付 ID
```

---

### 8.2 支付成功回调

**接口地址**：`POST /payment/callback/success`

**说明**：此接口由支付平台回调，前端无需调用

**请求参数**：
```typescript
{
  paymentNo: string;        // 支付单号（RequestParam）
  tradeNo: string;          // 第三方交易流水号（RequestParam）
}
```

**响应数据**：
```typescript
BaseResponse<boolean>
```

---

### 8.3 支付失败回调

**接口地址**：`POST /payment/callback/failed`

**说明**：此接口由支付平台回调，前端无需调用

**请求参数**：
```typescript
{
  paymentNo: string  // 支付单号（RequestParam）
}
```

**响应数据**：
```typescript
BaseResponse<boolean>
```

---

### 8.4 获取支付记录

**接口地址**：`GET /payment/get`

**权限要求**：需要登录

**请求参数**：
```typescript
{
  orderId: number  // 订单ID（Query参数）
}
```

**响应数据**：
```typescript
interface PaymentVO {
  id: number;
  paymentNo: string;
  orderId: number;
  paymentMethod: string;
  payAmount: number;
  payStatus: 0 | 1 | 2;      // 支付状态：0-待支付 1-已支付 2-已退款
  transactionId: string;
  payTime: string;
  createTime: string;
}

BaseResponse<PaymentVO>
```

---

## 九、评价模块 API

### 9.1 添加评价

**接口地址**：`POST /review/add`

**权限要求**：需要登录

**请求参数**：
```typescript
interface Review {
  productId: number;        // 商品ID
  orderId: number;          // 订单ID
  shopId: number;           // 店铺ID
  rating: 1 | 2 | 3 | 4 | 5;// 评分
  content: string;          // 评价内容
  images: string;           // 评价图片（JSON数组字符串）
  isAnonymous: 0 | 1;       // 是否匿名
}
```

**响应数据**：
```typescript
BaseResponse<number>  // 返回评价 ID
```

---

### 9.2 删除评价

**接口地址**：`POST /review/delete`

**权限要求**：需要登录，仅评价者或管理员可删除

**请求参数**：
```typescript
interface DeleteRequest {
  id: number;  // 评价ID
}
```

**响应数据**：
```typescript
BaseResponse<boolean>
```

---

### 9.3 分页查询商品评价

**接口地址**：`GET /review/list/page`

**请求参数**：
```typescript
{
  productId: number;        // 商品ID（Query参数）
  current?: number;         // 当前页码，默认 1
  pageSize?: number;        // 每页数量，默认 10
}
```

**响应数据**：
```typescript
interface ReviewVO {
  id: number;
  userId: number;
  userName: string;
  userAvatar: string;
  productId: number;
  rating: 1 | 2 | 3 | 4 | 5;
  content: string;
  images: string;           // JSON数组字符串
  isAnonymous: 0 | 1;
  likeCount: number;
  replyContent: string;     // 商家回复
  replyTime: string;
  createTime: string;
}

BaseResponse<Page<ReviewVO>>
```

---

## 十、收货地址模块 API

### 10.1 添加收货地址

**接口地址**：`POST /address/add`

**权限要求**：需要登录

**请求参数**：
```typescript
interface Address {
  receiverName: string;     // 收货人姓名
  receiverPhone: string;    // 收货人手机号
  province: string;         // 省份
  city: string;             // 城市
  district: string;         // 区/县
  detailAddress: string;    // 详细地址
  postalCode: string;       // 邮政编码
  isDefault: 0 | 1;         // 是否默认地址
}
```

**响应数据**：
```typescript
BaseResponse<number>  // 返回地址 ID
```

---

### 10.2 更新收货地址

**接口地址**：`POST /address/update`

**权限要求**：需要登录

**请求参数**：
```typescript
interface Address {
  id: number;               // 地址ID
  receiverName: string;
  receiverPhone: string;
  province: string;
  city: string;
  district: string;
  detailAddress: string;
  postalCode: string;
  isDefault: 0 | 1;
}
```

**响应数据**：
```typescript
BaseResponse<boolean>
```

---

### 10.3 删除收货地址

**接口地址**：`POST /address/delete`

**权限要求**：需要登录

**请求参数**：
```typescript
interface DeleteRequest {
  id: number;  // 地址ID
}
```

**响应数据**：
```typescript
BaseResponse<boolean>
```

---

### 10.4 设置默认地址

**接口地址**：`POST /address/setDefault`

**权限要求**：需要登录

**请求参数**：
```typescript
{
  addressId: number  // 地址ID（RequestParam）
}
```

**响应数据**：
```typescript
BaseResponse<boolean>
```

---

### 10.5 获取用户地址列表

**接口地址**：`GET /address/list`

**权限要求**：需要登录

**请求参数**：无

**响应数据**：
```typescript
interface AddressVO {
  id: number;
  receiverName: string;
  receiverPhone: string;
  province: string;
  city: string;
  district: string;
  detailAddress: string;
  postalCode: string;
  isDefault: 0 | 1;
}

BaseResponse<AddressVO[]>
```

---

## 十一、文件上传模块 API

### 11.1 文件上传（通用）

**接口地址**：`POST /file/upload`

**权限要求**：需要登录

**请求方式**：`multipart/form-data`

**请求参数**：
```typescript
{
  file: File  // 文件对象，最大 2MB，支持 jpeg/jpg/png/webp
}
```

**响应数据**：
```typescript
interface UploadPictureResult {
  picName: string;          // 图片名称
  picWidth: number;         // 图片宽度
  picHeight: number;        // 图片高度
  picScale: number;         // 图片宽高比
  picFormat: string;        // 图片格式
  picSize: number;          // 图片大小（字节）
  picColor: string;         // 图片主色调
  url: string;              // 图片访问URL
  thumbnailUrl: string;     // 缩略图URL
}

BaseResponse<UploadPictureResult>
```

**调用示例**：
```typescript
const uploadFile = async (file: File) => {
  const formData = new FormData();
  formData.append('file', file);

  const response = await axios.post<BaseResponse<UploadPictureResult>>(
    '/file/upload',
    formData,
    {
      headers: {
        'Content-Type': 'multipart/form-data'
      }
    }
  );

  return response.data;
};
```

---

### 11.2 上传商品图片

**接口地址**：`POST /file/upload/product`

**权限要求**：seller（卖家）

**请求方式**：`multipart/form-data`

**请求参数**：
```typescript
{
  file: File  // 文件对象
}
```

**响应数据**：
```typescript
BaseResponse<UploadPictureResult>
```

---

### 11.3 上传店铺图片

**接口地址**：`POST /file/upload/shop`

**权限要求**：seller（卖家）

**请求方式**：`multipart/form-data`

**请求参数**：
```typescript
{
  file: File  // 文件对象
}
```

**响应数据**：
```typescript
BaseResponse<UploadPictureResult>
```

---

### 11.4 上传用户头像

**接口地址**：`POST /file/upload/avatar`

**权限要求**：需要登录

**请求方式**：`multipart/form-data`

**请求参数**：
```typescript
{
  file: File  // 文件对象
}
```

**响应数据**：
```typescript
BaseResponse<UploadPictureResult>
```

---

### 11.5 根据URL删除文件

**接口地址**：`POST /file/deleteByUrl`

**权限要求**：需要登录

**请求参数**：
```typescript
{
  fileUrl: string  // 文件URL（RequestParam）
}
```

**响应数据**：
```typescript
BaseResponse<boolean>
```

---

## 十二、系统健康检查 API

### 12.1 健康检查

**接口地址**：`GET /health`

**请求参数**：无

**响应数据**：
```typescript
BaseResponse<string>  // "ok"
```

---

## 十三、数据字典

### 13.1 用户角色枚举

```typescript
enum UserRole {
  BUYER = 'buyer',      // 买家
  SELLER = 'seller',    // 卖家
  ADMIN = 'admin'       // 管理员
}
```

---

### 13.2 性别枚举

```typescript
enum Gender {
  UNKNOWN = 0,          // 未知
  MALE = 1,             // 男
  FEMALE = 2            // 女
}
```

---

### 13.3 店铺等级枚举

```typescript
enum ShopLevel {
  NORMAL = 0,           // 普通店
  BRAND = 1,            // 品牌店
  FLAGSHIP = 2          // 旗舰店
}
```

---

### 13.4 店铺状态枚举

```typescript
enum ShopStatus {
  PENDING = 0,          // 待审核
  OPEN = 1,             // 营业中
  CLOSED = 2,           // 已关闭
  REJECTED = 3          // 审核拒绝
}
```

---

### 13.5 商品状态枚举

```typescript
enum ProductStatus {
  PENDING = 0,          // 待上架
  ON_SALE = 1,          // 已上架
  OFF_SALE = 2          // 已下架
}
```

---

### 13.6 审核状态枚举

```typescript
enum ReviewStatus {
  PENDING = 0,          // 待审核
  APPROVED = 1,         // 通过
  REJECTED = 2          // 拒绝
}
```

---

### 13.7 订单状态枚举

```typescript
enum OrderStatus {
  UNPAID = 0,           // 待支付
  PAID = 1,             // 已支付
  PENDING_SHIP = 2,     // 待发货
  SHIPPED = 3,          // 已发货
  COMPLETED = 4,        // 已完成
  CANCELLED = 5,        // 已取消
  REFUNDING = 6,        // 退款中
  REFUNDED = 7          // 已退款
}
```

---

### 13.8 支付状态枚举

```typescript
enum PaymentStatus {
  UNPAID = 0,           // 未支付
  PAID = 1,             // 已支付
  REFUNDED = 2          // 已退款
}
```

---

### 13.9 物流状态枚举

```typescript
enum ShippingStatus {
  NOT_SHIPPED = 0,      // 未发货
  SHIPPED = 1,          // 已发货
  RECEIVED = 2          // 已签收
}
```

---

### 13.10 支付方式枚举

```typescript
enum PaymentMethod {
  ALIPAY = 1,           // 支付宝
  WXPAY = 2             // 微信支付
}
```

---

### 13.11 优惠券类型枚举

```typescript
enum CouponType {
  FULL_REDUCTION = 1,   // 满减
  DISCOUNT = 2,         // 折扣
  NO_THRESHOLD = 3      // 无门槛
}
```

---

### 13.12 优惠券状态枚举

```typescript
enum CouponStatus {
  NOT_STARTED = 0,      // 未开始
  ONGOING = 1,          // 进行中
  ENDED = 2             // 已结束
}
```

---

### 13.13 用户优惠券状态枚举

```typescript
enum UserCouponStatus {
  UNUSED = 0,           // 未使用
  USED = 1,             // 已使用
  EXPIRED = 2           // 已过期
}
```

---

## 十四、业务流程说明

### 14.1 用户注册登录流程

```
1. 用户注册
   ↓
2. 调用 /user/register 接口
   ↓
3. 注册成功后跳转到登录页
   ↓
4. 调用 /user/login 接口
   ↓
5. 登录成功，保存用户信息到 Pinia/LocalStorage
   ↓
6. 后续请求自动携带 Session Cookie
```

---

### 14.2 商品发布流程

```
1. 卖家上传商品图片（/file/upload/product）
   ↓
2. 填写商品信息
   ↓
3. 调用 /product/add 接口
   ↓
4. 商品进入待审核状态（reviewStatus = 0）
   ↓
5. 管理员调用 /product/review 接口审核
   ↓
6. 审核通过后商品可上架（status = 1）
```

---

### 14.3 下单支付流程

```
1. 用户浏览商品，添加到购物车（/cart/add）
   ↓
2. 查看购物车列表（/cart/list）
   ↓
3. 选择收货地址，创建订单（/order/create）
   ↓
4. 创建支付（/payment/create）
   ↓
5. 跳转到支付平台完成支付
   ↓
6. 支付成功后回调（/payment/callback/success）
   ↓
7. 订单状态更新为已支付（orderStatus = 1）
   ↓
8. 卖家发货，订单状态更新为已发货（orderStatus = 3）
   ↓
9. 用户确认收货（/order/confirm），订单状态更新为已完成（orderStatus = 4）
   ↓
10. 用户评价商品（/review/add）
```

---

### 14.4 店铺入驻流程

```
1. 用户注册并以买家身份登录
   ↓
2. 上传店铺Logo和Banner（/file/upload/shop）
   ↓
3. 填写店铺信息，调用 /shop/add 接口申请入驻
   ↓
4. 店铺进入待审核状态（reviewStatus = 0）
   ↓
5. 管理员调用 /shop/review 接口审核
   ↓
6. 审核通过后，用户角色变更为卖家（userRole = 'seller'）
   ↓
7. 店铺状态变更为营业中（shopStatus = 1）
```

---

## 十五、前端开发建议

### 15.1 Axios 配置示例

```typescript
import axios from 'axios';

// 创建 axios 实例
const request = axios.create({
  baseURL: 'http://localhost:8101',
  timeout: 10000,
  withCredentials: true,  // 携带 Cookie
});

// 请求拦截器
request.interceptors.request.use(
  (config) => {
    // 可以在这里添加 token 等
    return config;
  },
  (error) => {
    return Promise.reject(error);
  }
);

// 响应拦截器
request.interceptors.response.use(
  (response) => {
    const res = response.data;

    // 根据业务需求处理响应
    if (res.code === 0) {
      return res;
    } else {
      // 处理错误
      console.error(res.message);

      // 未登录，跳转到登录页
      if (res.code === 40100) {
        window.location.href = '/login';
      }

      return Promise.reject(new Error(res.message || 'Error'));
    }
  },
  (error) => {
    console.error('请求错误:', error);
    return Promise.reject(error);
  }
);

export default request;
```

---

### 15.2 API 封装示例

```typescript
// api/user.ts
import request from '@/utils/request';

export const userApi = {
  // 用户注册
  register: (data: UserRegisterRequest) => {
    return request.post<BaseResponse<number>>('/user/register', data);
  },

  // 用户登录
  login: (data: UserLoginRequest) => {
    return request.post<BaseResponse<User>>('/user/login', data);
  },

  // 获取当前登录用户
  getLoginUser: () => {
    return request.get<BaseResponse<User>>('/user/get/login');
  },

  // 用户注销
  logout: () => {
    return request.post<BaseResponse<boolean>>('/user/logout');
  },
};

// api/product.ts
export const productApi = {
  // 添加商品
  add: (data: ProductAddRequest) => {
    return request.post<BaseResponse<number>>('/product/add', data);
  },

  // 分页查询商品
  listPage: (data: ProductQueryRequest) => {
    return request.post<BaseResponse<Page<ProductVO>>>('/product/list/page', data);
  },

  // 获取商品详情
  get: (id: number) => {
    return request.get<BaseResponse<ProductVO>>('/product/get', { params: { id } });
  },
};

// api/file.ts
export const fileApi = {
  // 上传文件
  upload: (file: File) => {
    const formData = new FormData();
    formData.append('file', file);
    return request.post<BaseResponse<UploadPictureResult>>(
      '/file/upload',
      formData,
      {
        headers: {
          'Content-Type': 'multipart/form-data'
        }
      }
    );
  },
};
```

---

### 15.3 Pinia Store 示例

```typescript
// stores/user.ts
import { defineStore } from 'pinia';
import { ref } from 'vue';
import { userApi } from '@/api/user';

export const useUserStore = defineStore('user', () => {
  const userInfo = ref<User | null>(null);
  const isLogin = ref(false);

  // 登录
  const login = async (data: UserLoginRequest) => {
    const res = await userApi.login(data);
    if (res.code === 0) {
      userInfo.value = res.data;
      isLogin.value = true;
      localStorage.setItem('userInfo', JSON.stringify(res.data));
    }
    return res;
  };

  // 注销
  const logout = async () => {
    await userApi.logout();
    userInfo.value = null;
    isLogin.value = false;
    localStorage.removeItem('userInfo');
  };

  // 获取登录用户
  const fetchLoginUser = async () => {
    const res = await userApi.getLoginUser();
    if (res.code === 0) {
      userInfo.value = res.data;
      isLogin.value = true;
    }
  };

  // 初始化用户信息
  const initUser = () => {
    const localUser = localStorage.getItem('userInfo');
    if (localUser) {
      userInfo.value = JSON.parse(localUser);
      isLogin.value = true;
    }
  };

  return {
    userInfo,
    isLogin,
    login,
    logout,
    fetchLoginUser,
    initUser,
  };
});
```

---

### 15.4 路由守卫示例

```typescript
// router/index.ts
import { createRouter, createWebHistory } from 'vue-router';
import { useUserStore } from '@/stores/user';

const router = createRouter({
  history: createWebHistory(),
  routes: [
    {
      path: '/login',
      name: 'Login',
      component: () => import('@/views/Login.vue'),
    },
    {
      path: '/user/profile',
      name: 'UserProfile',
      component: () => import('@/views/user/Profile.vue'),
      meta: { requiresAuth: true },  // 需要登录
    },
    {
      path: '/shop/manage',
      name: 'ShopManage',
      component: () => import('@/views/shop/Manage.vue'),
      meta: { requiresAuth: true, role: 'seller' },  // 需要卖家权限
    },
    {
      path: '/admin',
      name: 'Admin',
      component: () => import('@/views/admin/Index.vue'),
      meta: { requiresAuth: true, role: 'admin' },  // 需要管理员权限
    },
  ],
});

// 路由守卫
router.beforeEach((to, from, next) => {
  const userStore = useUserStore();

  // 需要登录的页面
  if (to.meta.requiresAuth) {
    if (!userStore.isLogin) {
      next('/login');
      return;
    }

    // 检查角色权限
    if (to.meta.role && userStore.userInfo?.userRole !== to.meta.role) {
      alert('无权限访问');
      next('/');
      return;
    }
  }

  next();
});

export default router;
```

---

## 十六、注意事项

### 16.1 Session 认证

后端使用 Spring Session + Redis 管理用户会话，前端需要：
- 所有请求都携带 Cookie（设置 `withCredentials: true`）
- 无需手动管理 Token
- Session 过期后需要重新登录

---

### 16.2 文件上传限制

- 文件大小：最大 2MB
- 支持格式：jpeg、jpg、png、webp
- Content-Type：multipart/form-data

---

### 16.3 分页查询

- 默认 `current = 1`，`pageSize = 10`
- 最大每页数量建议不超过 100
- 排序字段需要与实体字段名一致

---

### 16.4 JSON 字段处理

以下字段以 JSON 字符串存储，需要前端序列化/反序列化：
- `product.imageUrls`：图片URL数组
- `product.tags`：标签数组
- `product.specifications`：商品规格对象
- `review.images`：评价图片数组
- `cart.specification`：购物车商品规格

**示例**：
```typescript
// 序列化
const imageUrls = ['url1', 'url2', 'url3'];
productData.imageUrls = JSON.stringify(imageUrls);

// 反序列化
const imageUrls = JSON.parse(productVO.imageUrls);
```

---

### 16.5 权限控制

接口权限分为三类：
- **公开接口**：无需登录
- **登录接口**：需要用户登录
- **角色接口**：需要特定角色（seller/admin）

前端需要根据用户角色显示/隐藏功能模块。

---

### 16.6 错误处理

建议在 axios 响应拦截器中统一处理错误：
- `40100`：未登录，跳转登录页
- `40101`：无权限，提示用户
- `50000`：系统错误，显示错误提示

---

## 十七、联系方式

如有问题，请联系后端开发团队。

---

**文档结束**